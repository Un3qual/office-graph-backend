# Production Read-Modify-Write Audit

Every production mutating function changed by this OpenSpec change must be
entered here before its persistence implementation changes. The audit records
the persisted reads that influence the write, the Ash concurrency safeguard
that owns the invariant, and the behavior or concurrency test that proves it.
Rows remain in this table after implementation so the final review can verify
that no touched invariant silently fell back to application-level
read-modify-write behavior.

| Owner and function | Persisted reads and invariant | Ash safeguard | Verification |
| --- | --- | --- | --- |
| `OfficeGraph.Tenancy.ensure_local_scope/1` | Reads or creates the organization by slug, then its workspace and initiative by parent-scoped slug, plus the default workstream. Exactly one row for each identity must survive concurrent first bootstrap attempts, and all returned records must belong to the same hierarchy. | One transactional generic action declares every touched tenancy resource. Each create uses an Ash upsert action backed by the existing identity and changes no fields on conflict. PostgreSQL 18 `MERGE` can surface a unique violation when two statements concurrently observe the same missing identity, so the boundary retries the complete action once only for the four declared tenancy identity constraints; the failed action has already rolled back before retry. | `OfficeGraph.Foundation.BootstrapTest` proves stable IDs on repeat bootstrap. `OfficeGraph.Integrations.IntakeBootstrapConcurrencyTest` proves two independent first-scope attempts return the same IDs and one row per identity. |
| `OfficeGraph.Content.persist_plain_document/3` | Performs no decision-making persisted read. The invariant is failure atomicity across the document, initial paragraph block, and first revision. | A transactional generic action on `Document` declares the block and revision resources it touches. Any nested Ash create error is returned to the action so Ash rolls back the full action-owned transaction. | `OfficeGraph.WorkGraph.PersistenceTest` proves the document, block, and revision are created as one domain operation and that invalid or unauthorized operation contexts persist none of them. |
| `OfficeGraph.Authorization.ensure_owner_role/2` | Ensures the complete recognized capability catalog, the organization owner role, every owner capability grant, the exact principal/workspace assignment, and policy bundle version 1. Concurrent owner bootstraps must converge without partial grants or duplicate assignments. | One transactional generic action on `Role` declares every authorization resource it touches. Identity-backed Ash upserts preserve existing rows. The boundary retries the complete rolled-back action once only for the declared authorization identity constraints when PostgreSQL 18 `MERGE` loses an initially-missing identity race. | `OfficeGraph.Foundation.BootstrapTest` proves repeat stability and exact grants. `OfficeGraph.Integrations.IntakeBootstrapConcurrencyTest` proves two independent first-owner attempts converge on one assignment and policy bundle. Authorization capability migration suites prove repeat bootstrap repairs missing grants. |
| `OfficeGraph.Authorization.persist_system_role/4` | Ensures the principal/scope-specific system role, requested capability rows, grants, and the nullable workspace assignment. Organization-wide and workspace assignments must remain distinct while repeated and concurrent setup converges. | A transactional generic action on `Role` uses the same identity-backed Ash upserts. `RoleAssignment.unique_assignment` uses `nils_distinct?: false`, so Ash matches nullable workspace identity without partial-index fragments. The same bounded full-action retry handles only declared authorization identity conflicts. | `OfficeGraph.Authorization.SystemRoleTest` proves organization/workspace separation, repeat setup, classified storage failure, and authorization. The owner-bootstrap concurrency suite covers the shared authorization identity race machinery. |
| `OfficeGraph.Identity.ensure_owner/1` | Ensures one principal by exact owner email and one profile by principal. Concurrent local-owner bootstrap must return the same records without leaving a principal without its profile. | A transactional generic action on `Principal` declares `PrincipalProfile` as touched and uses identity-backed create upserts. A single full-action retry is limited to the principal/profile identity constraints after a PostgreSQL 18 `MERGE` conflict has rolled the action back. | `OfficeGraph.Foundation.BootstrapTest` proves stable owner/profile IDs. `OfficeGraph.Integrations.IntakeBootstrapConcurrencyTest` forces two first-owner attempts and proves one principal/profile pair survives. |
| `OfficeGraph.Identity.ensure_session_context/3` | Ensures the one active `local_owner` session for a principal and exact organization/workspace while preserving revoked-session replacement behavior. | The `Session.unique_context` identity owns the partial active-session match; a dedicated Ash upsert action returns the existing active session. A bounded retry handles only its declared unique index race. | `OfficeGraph.Foundation.BootstrapTest` proves repeat session stability and replacement after revocation. The owner-bootstrap race test proves concurrent first session issuance converges. |
| `OfficeGraph.Identity.ensure_system_principal/2` | Ensures one principal per exact email, then verifies the stored kind and active status match the requested system identity. | The principal email identity owns the upsert; no get-then-insert window remains. The same bounded identity-conflict retry re-runs only the single Ash create action. | `OfficeGraph.Authorization.SystemRoleTest` and agent/GitHub integration tests prove repeat system-principal setup and kind/status rejection. |
| `OfficeGraph.Identity.store_oidc_login_transaction/1` | Deletes expired login guards and creates one new database-generated transaction ID in the same operation. Live guards must remain. | A transactional generic action reads expired guards and destroys them through Ash before creating the new guard. Any failure rolls back both pruning and creation. | `OfficeGraph.AuthenticationTest` proves expired guards are pruned while live and newly created guards remain. |
| `OfficeGraph.Identity.consume_oidc_login_transaction/1` | Reads the exact guard, decides whether it is unexpired, and consumes it exactly once. Concurrent callbacks must have one winner. | A transactional generic action performs an Ash `FOR UPDATE` read, destroys the locked row, and returns the validity boolean as a successful action result so expired-row deletion commits. The boundary maps `false` to the public invalid-transaction error after commit. | `OfficeGraph.AuthenticationTest` proves one of two concurrent callbacks succeeds, replay fails, and expired guards fail before provider exchange. |
| `OfficeGraph.Identity.ExternalIdentityReconciliation.reconcile_with_evidence/2` | Reads the provider-subject binding, every link for the normalized verified email, and every principal matching that normalized email before deciding whether to authenticate or persist review. A known subject must remain stable, competing subjects must not both become active, and review outcomes must commit. | One transactional generic action locks normalized-email principals and links plus the exact provider-subject row through Ash `FOR UPDATE` queries in deterministic row order. The normalized match uses Ash string expressions. A provider-subject identity-backed unique conflict retries the complete rolled-back action once. A typed action result represents authentication or committed review without using an error to escape the action transaction. | `OfficeGraph.Identity.HumanAuthenticationTest` proves exact-subject stability, normalized and ambiguous email behavior, conflicting-subject review isolation, lifecycle rejection, and persisted review evidence. The identity concurrency additions cover simultaneous provider-subject reconciliation. |
| `OfficeGraph.Identity.HumanSessions.issue/4` | Revalidates and locks the principal and external-identity link, then reads and replaces any active human-web session for the exact principal and tenant scope. Identity disablement must win before issuance, and session replacement plus login/revocation evidence must be failure-atomic. | A transactional generic action on `Session` first locks the principal row, which serializes all issuance for that principal including a first session with no row to lock. It then locks the exact identity link and active same-scope sessions through Ash `FOR UPDATE` queries before performing Ash updates and creates. A typed result preserves classified lifecycle rejection without treating it as a persistence failure. | `OfficeGraph.Identity.HumanAuthenticationTest` proves identity revalidation, one active replacement, exact event evidence, and storage-failure classification. The identity concurrency additions prove simultaneous same-scope issuance serializes to one active session. |
| `OfficeGraph.Identity.HumanSessions.revoke/2` | Reads the exact human-web session and conditionally revokes it. Repeated or concurrent logout must create at most one revocation transition and one logout event. | A transactional generic action locks the exact session row through Ash `FOR UPDATE`; the lock is held through the Ash update and authentication-event create. Missing, wrong-purpose, active, and already-revoked states are typed successful action outcomes, while persistence errors roll back the transition and event together. | `OfficeGraph.Identity.HumanAuthenticationTest` proves logout idempotency, revoked-session rejection, and exact bounded logout evidence. The identity concurrency additions cover simultaneous repeated logout. |
| `OfficeGraph.Integrations.ensure_provider_source/2` | Ensures one provider source for the exact kind/key identity and preserves the first stored source record on replay. | A dedicated Ash create action upserts on `ExternalSource.unique_kind_key`, updates no fields on conflict, and returns the skipped record. A bounded complete-action retry handles only the declared source unique constraint if PostgreSQL 18 `MERGE` loses an initially-missing identity race. | GitHub installation, webhook, reconciliation, and product-mapping suites prove stable provider-source identity and conflict classification. |
| `OfficeGraph.Integrations.archive_system_delivery/3` | Reads the exact source-scoped provider delivery before deciding whether to create or replay its immutable archive. Matching delivery content and scope must replay; changed content or operation scope must conflict. | A transactional generic action locks the existing `ExternalSource` row through Ash `FOR UPDATE`, then reads or creates the provider-delivery archive. The stable source row serializes even the first archive for a delivery identity, eliminating the partial-index fragment. A bounded retry handles only the declared provider-delivery constraint as defense against older non-locking callers. | GitHub webhook receipt and worker suites prove created/replayed status, immutable-content conflict, exact tenant scope, and provider delivery lookup. |
| `OfficeGraph.Integrations.submit_or_replay_manual_intake_command/3` and `record_manual_intake/3` | Reads the exact operation replay, canonical manual source, accepted normalized event, immutable archive content, and existing proposed-change set before returning replay, conflict, accepted, or duplicate. One command operation must have one intake result; one source/replay identity must have one accepted event; archive, event, proposals, durable event, and job must commit together. | One transactional generic action locks command operations through Ash `FOR UPDATE`; legacy non-command operations retain their independent duplicate semantics without an unnecessary aggregate lock. The source is created by an identity-backed Ash upsert, and `NormalizedIntakeEvent.accepted_replay_key` selects the single accepted winner without a partial-index fragment in application code. Nested owning Ash actions create the archive/event/proposals and durable delivery in the same transaction. A bounded complete-action retry is limited to the source and accepted-replay identities; after the losing action rolls back, the retry returns a duplicate or typed content conflict from the committed winner. | Manual-intake replay, durable-delivery, command replay, source race, content-conflict, proposed-change failure, and intake concurrency suites prove exact replay, one accepted winner, typed conflict, source convergence, and failure atomicity. |
| `OfficeGraph.ProposedChanges.create_for_manual_intake/4` | Locks and revalidates the accepted normalized event, then reads its proposal set before either returning the exact complete set or creating the four required proposal types. A partial proposal set must never commit or be treated as replay. | A transactional generic action on `ProposedGraphChange` locks the normalized event through Ash `FOR UPDATE`. The existing normalized-event/change-type identity remains the database backstop, while the locked parent serializes the absent-set decision and all four Ash creates. Typed rejected action results preserve exact validation errors without committing partial writes. | Proposed-change behavior tests prove exact event/operation/scope validation and idempotent complete-set replay; integration failure-atomicity proves failed creation leaves no partial set. `OfficeGraph.Integrations.EvidenceConcurrencyTest` proves independent first-set creators return the same complete four-row set. |
| `OfficeGraph.ProposedChanges.apply_all/3` | Reloads and locks the exact ordered proposal IDs, validates tenant scope, required types, accepted-event ownership, pending or exact replay state, then creates the graph lineage and marks every proposal with its applied operation/resource. No graph truth or partial proposal lifecycle may survive a later failure. | A transactional generic action owns authorization decision recording, deterministically ordered locked proposal reads, nested WorkGraph actions, and internal lifecycle updates. A typed action result treats validation and authorization denials as committed business outcomes so denial evidence survives, while unexpected persistence failures fail the action and roll back the full graph/proposal mutation. | `OfficeGraph.ProposedChangesTest` proves validation precedence, authorization evidence, apply-only execution, exact replay, graph lineage, failure rollback, and pending-state preservation. `OfficeGraph.Integrations.WorkflowStateConcurrencyTest` proves separate owners produce one graph winner, one typed stale loser, and one applied-operation identity. |
| `OfficeGraph.WorkPackets.create_packet/3` | Reads the exact operation replay, required verification checks, and source/check pairing before deciding whether to replay or atomically create the packet, first immutable version, ordered children, and current-version pointer. No incomplete packet contract may commit. | A transactional generic action on `WorkPacket` locks the operation through Ash `FOR UPDATE`, so absent-record creation and replay classification serialize on a stable parent. `WorkPacket.unique_operation` remains the database identity backstop; all nested Ash creates, bulk child creation, and the current-version update participate in the owning action transaction. | Work-packet contract tests prove typed validation, exact replay/conflict behavior, child ordering, and failure rollback. `OfficeGraph.Integrations.CommandReplayConcurrencyTest` proves separate connection owners replay one packet/version identity for the same operation. |
| `OfficeGraph.WorkPackets.create_version/4` | Locks and reloads the packet and its current version before checking the caller's expected version, source/check contract, existing operation replay, next version number, ordered children, and current-version pointer. Two writers must not both advance one packet from the same current version. | A transactional generic action on `WorkPacket` locks both the operation and the scoped packet through Ash `FOR UPDATE`. The packet row serializes version-number allocation and pointer advancement, while `WorkPacketVersion.unique_packet_version` backs the invariant. Any replay conflict, stale expected version, child failure, or pointer failure returns an action error and rolls back the full version contract. | Work-packet contract tests prove stale-version rejection, exact operation replay, ordered child reconstruction, and failure atomicity. `OfficeGraph.Integrations.WorkflowStateConcurrencyTest` proves separate same-version writers produce one version-two winner and one typed stale loser. |
| `OfficeGraph.Runs.start_run/4` | Reads the exact operation replay, scoped packet/version contract, current-version pointer, active runs, and ordered required checks before creating one run and its check snapshot. Two writers must not both start active runs for one packet version, and no partial snapshot may commit. | A transactional generic action on `Run` locks the operation and the owning packet through Ash `FOR UPDATE`. The packet lock serializes the active-run decision, `Run.unique_operation` backs command replay, and the resource create validation rechecks the current ready packet contract before bulk child creation. | Work-run contract tests prove stale packet rejection, active-run conflict, exact replay, required-check ordering, query bounds, and rollback. `OfficeGraph.Integrations.CommandReplayConcurrencyTest` proves same-operation replay, while `OfficeGraph.Integrations.WorkflowStateConcurrencyTest` proves distinct operations produce one active-run winner and one typed loser. |
| `OfficeGraph.Runs.record_observation/4` | Reads operation replay and the source/idempotency identity, locks the exact run, creates or replays one immutable observation, then derives the run lifecycle from all committed observations. The same operation or source identity must not create two observations or stale-overwrite terminal run state. | A transactional generic action locks the operation and run through Ash `FOR UPDATE`. `ExecutionObservation.unique_operation` and `unique_source_idempotency_key` replace the advisory lock; source replay uses an Ash identity upsert with no mutable fields, followed by exact immutable comparison. Because PostgreSQL 18 `MERGE` can surface a unique violation when separate run owners concurrently observe the same missing source identity, the boundary retries the complete rolled-back action once only for the declared source identity constraint. The run lock serializes lifecycle reduction, and any unexpected create/update failure rolls back observation plus run mutation. | Observation contract tests prove operation replay, source replay/conflict, lifecycle reduction, preflight behavior, and failure atomicity. The separate-owner concurrency suite proves the cross-operation source-identity race has one winner and one typed loser. |
| `OfficeGraph.Runs.record_agent_observation/5` | Creates or replays the exact execution/step source identity for one non-authoritative agent observation. A changed context package or summary for the same execution step must conflict without mutating run state. | The observation create uses an Ash upsert backed by `ExecutionObservation.unique_source_idempotency_key` with no mutable upsert fields, followed by exact immutable comparison. It returns no run lifecycle update and propagates typed replay conflict to the enclosing agent-output action transaction, whose execution-step owner already serializes routed effects. | Agent output routing tests prove governed observation creation, exact replay, conflict rollback, and unchanged run state. The standalone separate-owner observation race exercises the shared source-identity upsert and PostgreSQL 18 retry path. |
| `OfficeGraph.Runs.apply_accepted_verification_result/2` and `apply_waived_verification_result/2` | Locks the run and required-check set before applying passed, failed, or waived verification state and recomputing aggregate run lifecycle. A terminal verified run must not be overwritten by a later failure, and concurrent child results must not derive from stale siblings. | A transactional generic action on `Run` locks the parent first, then locks required checks in stable order, updates the exact check, and derives the parent from the locked set. The parent lock serializes all verification outcomes; unexpected nested failures roll back child and parent together, while typed terminal conflicts remain explicit. | Verification, waiver, and work-run evidence suites prove check transition, terminal-state protection, aggregate recomputation, replay, and rollback. `OfficeGraph.Integrations.EvidenceConcurrencyTest` proves concurrent sibling acceptance reaches verified exactly once, and `OfficeGraph.Integrations.WorkflowStateConcurrencyTest` proves waiver-versus-acceptance produces one result and one typed loser. |
| `OfficeGraph.Verification.create_evidence_candidate/3` | Reads the exact operation replay plus scoped verification-check, run, observation, and artifact references before creating one immutable candidate. One command operation must not create two candidates or replay changed evidence facts. | A transactional generic action on `EvidenceCandidate` locks the operation through Ash `FOR UPDATE`; `EvidenceCandidate.unique_operation` remains the database identity backstop. All reference validation runs inside the owning action before the Ash create. Modeled Ash reads constrain every supplied operation, required-check, verification-check, and observation identity to the exact tenant and relationship contract, return field-specific validation errors on storage failure, and preserve typed replay conflicts without a write. | Evidence-candidate contract and transport tests prove scoped references, derived candidate state, actor-operation binding, graph-only observation matching, exact operation replay, changed-input conflict, and command authorization. `OfficeGraph.Integrations.EvidenceConcurrencyTest` proves separate connection owners replay one candidate identity for identical operation input, return one typed loser for changed input on one operation, and reject a concurrent cross-workspace artifact reference without suppressing the valid writer or persisting the rejected candidate. |
| `OfficeGraph.Verification.accept_evidence_candidate/4` | Locks and revalidates the exact candidate, run or runless verification completion aggregate, result slot, optional observation/artifact, and command replay before creating the document, graph item, evidence item, relationships, verification result, traces, candidate transition, and parent lifecycle updates. No partial evidence graph or stale parent transition may commit. | A transactional generic action on `EvidenceCandidate` locks the operation and candidate first. Run-backed acceptance locks the run, which serializes its check result and lifecycle. Runless acceptance uses the WorkGraph completion lock order—task, ordered findings, then ordered checks—so it serializes the check-wide result slot without deadlocking a direct completion. All validation precedes the first write, and every nested persistence failure fails the action so the complete evidence graph rolls back. | Work-run evidence and transport suites prove result policy, exact replay/conflict, slot conflict, runless and run-backed propagation, graph lineage, terminal-state protection, and unchanged state on rejection. The separate-owner concurrency suite proves deterministic runless acceptance versus direct completion and run-backed competing-result behavior. |
| `OfficeGraph.Verification.waive_required_check/5` | Locks the operation, run, and ordered required-check set before validating expected run state, exact check membership, waiver replay, and pending state, then creates one waiver result and recomputes run lifecycle. A stale waiver must not consume a check or overwrite a concurrent result. | A transactional generic action on `VerificationResult` locks the operation and run aggregate through Ash `FOR UPDATE`, then locks required checks in stable order. Operation replay is compared against the result-slot-constrained waiver row, the run lock serializes acceptance and waiver lifecycle decisions, and nested result/check/run/tracing failures fail the owning action. | Waiver contract and transport tests prove exact replay, changed-input conflict, stale-state rejection, wrong-check rejection, pending-sibling preservation, traces, and lifecycle recomputation. `OfficeGraph.Integrations.WorkflowStateConcurrencyTest` proves waiver-versus-acceptance commits one result and returns one typed slot/state loser. |
| `OfficeGraph.AgentRuntime.bind_run_review_agent/2` | Reads the exact operation, canonical agent definition, organization/workspace binding scope, principal, and role before creating or replaying the binding contract. Repeated commands must return the same immutable binding, while changed scope, inactive definitions, or incompatible lifecycle state must fail without partial authorization setup. | A transactional generic action on `OrganizationBinding` locks the operation and canonical definition through Ash `FOR UPDATE`. The declared binding-scope identity is used by an Ash upsert with no mutable fields, while nested principal, role, assignment, and capability actions remain inside the owning transaction. A typed result distinguishes creation from exact replay without using an error to escape the transaction. | Organization-binding tests prove authorization, exact replay with the same or a new operation, organization/workspace isolation, inactive-definition rejection, and full rollback after a nested failure. The separate-owner agent-runtime suite proves competing first binds converge on one binding and principal. |
| `OfficeGraph.AgentRuntime.InvocationCommands.invoke/3` and `invoke_system/2` | Reads and revalidates the operation, scoped binding, active definition, invocation execution identity, authority snapshot, and context package before creating the execution and its initial worker operation. One operation or binding/run/idempotency identity must resolve to one immutable invocation contract, and changed replay input must conflict. | A transactional generic action on `AgentExecution` locks the operation, binding, and definition through Ash `FOR UPDATE`. Existing operation and binding/run/idempotency identities back exact replay; the latter is created through an Ash upsert with no mutable fields. Execution, authority snapshot, context package and entries, initial graph work, and typed replay classification all remain in one action-owned transaction. | Invocation contract tests prove exact replay, changed-input conflict, scope and lifecycle validation, authority and context snapshots, transport behavior, and rollback after dependent-write failure. The separate-owner agent-runtime suite proves one operation concurrently resolves to one execution and context package. |
| `OfficeGraph.AgentRuntime.ApprovalCommands.resolve/6` | Reads and locks the exact operation, tenant-scoped approval request, captured waiting execution version, and active model request before approving, denying, cancelling, or replaying the decision. A stale or expired decision must not resume work, and the request, execution, traces, events, and resume job must remain failure-atomic. | A transactional generic action on `ApprovalRequest` owns the lock order and all dependent writes. The request row serializes its versioned decision, while `AgentExecution.transition` applies Ash's optimistic `state_version` lock. Typed action results preserve exact replay and classified conflicts, and action rollback covers nested model-request, trace, delivery, and Oban writes. | Approval command and transport tests prove approve, deny, cancel, expiry, stale-version rejection, exact replay, one resume job, durable evidence, and rollback. The separate-owner agent-runtime suite proves competing decisions produce one committed resolution and one typed stale loser. |
| `OfficeGraph.AgentRuntime.ContextExpansionCommands.resolve/6` | Reads and locks the exact operation, tenant-scoped expansion request, captured waiting execution, authority snapshot, and current immutable package before resolving or replaying the decision. An approval must create exactly one successor package and resume only the captured step; stale, expired, or changed decisions must not mutate lineage. | A transactional generic action on `ContextExpansionRequest` owns the aggregate locks and dependent writes. The request row serializes its versioned decision, `AgentExecution.transition` applies Ash's optimistic `state_version` lock, and successor package, entries, traces, events, and resume job commit inside the same action. | Context-expansion command and transport tests prove approve, deny, cancel, expiry, stale-version rejection, immutable successor lineage, exact replay, one resume job, durable evidence, and rollback. The separate-owner agent-runtime suite proves competing decisions produce one committed resolution and one typed stale loser. |
| `OfficeGraph.AgentRuntime.CancellationCommands.cancel/3` | Reads and locks both pending gate kinds before the exact tenant-scoped execution and active model request, then checks durable operation replay and expected execution version. Cancellation must retire only the matching captured gate, terminalize the execution/request once, and signal the persisted adapter only after commit. | A transactional generic action on `AgentExecution` preserves the gate-before-execution lock order, uses the execution's Ash optimistic `state_version` transition, and atomically records gate/request terminal state plus durable invalidations. Its typed result carries the immutable model-request adapter identity for post-commit signaling and exact lost-response replay. | Execution-worker cancellation tests prove queued, running, approval-waiting, and context-waiting cancellation, stale and terminal conflicts, exact replay, gate retirement, durable invalidation, and post-commit adapter signaling. The separate-owner agent-runtime suite proves duplicate cancellation has one winner and one typed stale loser. |
| `OfficeGraph.AgentRuntime.GateExpiryWorker.perform/1` | Reads and locks one opaque approval or context-expansion request and its execution before deciding to snooze, no-op, expire, or supersede. Expiry may fail the execution only when tenant, step, waiting state, and captured execution version still match. | Each request resource owns a transactional expiry action. The locked request serializes competing resolution/cancellation/expiry, the locked execution plus Ash optimistic transition prevents stale terminalization, and typed action output maps committed completion or snooze back to Oban without using an error as control flow. | Approval and context-expansion expiry tests prove pre-expiry snooze, resolved-gate no-op, exact waiting expiry, stale-wait supersession, durable failure evidence, and unchanged unrelated work. |
| `OfficeGraph.AgentRuntime.ExecutionWorker.perform/1` | Loads the exact job-scoped execution, immutable authority snapshot and current context package, definition, snapshotted credentials, adapter manifest, and system operation. Claim locks the operation and execution before creating or replaying the step request or a gate; completion and finalization lock the operation, execution, and exact model request before routing output or applying retry, failure, cancellation, or completion. A lease owner must not dispatch or finalize after cancellation, supersession, or expiry. | Four transactional generic actions on `AgentExecution` own claim, completion, pre-claim failure, and finalization. Stable execution/request identities plus Ash `FOR UPDATE` reads serialize first request and gate creation, while `AgentExecution.transition` applies the optimistic `state_version` lock to every lifecycle mutation. Adapter invocation remains outside the transaction. A bounded typed execution/request posture read immediately before dispatch replaces the raw join and fails stale. Typed action results represent run, leased, waiting, terminal, and completed outcomes. | The full execution-worker suite proves immutable adapter and credential provenance, approval and context gates, duplicate dispatch suppression, active and expired lease behavior, restart recovery, retry exhaustion, cancellation, authority drift, routed-output rollback, classified terminal outcomes, and tenant scope. The separate-owner agent-runtime suite proves duplicate claim, retry, and terminal races share one model request, consume one attempt, and defer the competing owner to the active lease. |
| `OfficeGraph.GitHubIntegration.bind_installation/2` and `bind_installation/3` | Reads and revalidates the operation, installation external identity, service and webhook principals, scoped system roles, permission snapshot, permission entries, and organization- or workspace-scoped credential references before creating or replaying a complete installation binding. One external installation must have one owner, and any losing claim must not leave principals, roles, permissions, credentials, or bindings behind. | A transactional generic action on `Installation` locks the operation through Ash `FOR UPDATE`, creates or replays the installation through `unique_external_installation`, and validates the immutable stored contract. Integration credentials use their declared partial Ash identities directly: `unique_organization_reference` for organization scope and `unique_workspace_reference` for workspace scope. All dependent writes remain in the installation action, and a typed rollback marker preserves `:forbidden` while rolling back a losing external-ID claim. A bounded complete-action retry handles only the three declared installation/credential identity constraints when PostgreSQL 18 reports a concurrent initially-missing identity conflict. | Installation binding and API suites prove exact replay, changed-input conflict, cross-tenant ownership rejection, shared credential reuse, scoped authorization, safe secret metadata, storage-failure normalization, and full rollback after nested authorization, role, or installation failures. The separate-owner GitHub integration suite proves one external-ID winner, one stable forbidden loser, and no duplicate installation. |
| `OfficeGraph.GitHubIntegration.WebhookReceipt.accept/2` | Selects an active installation and webhook credential before signature verification, then must revalidate both persisted records before recording the verified delivery. Revocation or credential lifecycle changes between verification and persistence must prevent receipt effects, while duplicate deliveries must return the immutable original receipt without recreating jobs. | After all request-only validation, one transactional generic action on `Installation` locks the exact installation, webhook binding, and active credential through Ash `FOR UPDATE`. System operation, provider source, immutable archive, domain event, dispatch job, and reconciliation jobs are created inside that action. The provider-delivery archive identity and locked provider source serialize duplicate delivery decisions; replay creates no new jobs. | Webhook receipt tests prove invalid requests have no effects, exact duplicate replay, one job set, multi-PR dispatch, credential/storage failure classification, and rollback of provider-source/archive failures. The separate-owner GitHub integration suite proves a concurrent delivery and replay produce one archive, operation, event, and job. |
| `OfficeGraph.GitHubIntegration.OutboundCommands.reply_to_review/3` and `update_check/3` | Reads the operation replay, active installation and current permission, target resource and GitHub extension, selected-installation reconciliation provenance, and target provider version before creating an outbound action. A permission, lifecycle, provenance, or version change must win before a provider mutation is queued, and one command operation must enqueue only one action. | One transactional generic action on `OutboundAction` locks the operation, installation, permission row, target, provider extension, and sync outcome through Ash `FOR UPDATE`, then rechecks authorization and the expected provider version. The operation lock serializes the absent-action decision; action, unique Oban job, audit, and revision commit inside the owning action. Exact replay returns the stored action without another job. | Outbound command and API suites prove permission, installation, provenance, stale-version, tenant, exact replay, one-job, and persistence rollback behavior. The separate-owner GitHub integration suite proves concurrent command replay returns one action and enqueues one job. |
| `OfficeGraph.GitHubIntegration.Reconciler.reconcile/2`, `finalize_failure/3`, and `exhaust_pre_operation/4` | Reads installation and credential authority before provider access, then reconciles provider-neutral resources, GitHub extensions, external references, product signals, traces, installation lifecycle, and one operation-scoped SyncOutcome. Concurrent success, retry, revocation, and terminalization must not stale-overwrite one another, and provider identities shared by distinct deliveries must converge. | Transactional generic actions on `SyncOutcome` lock the operation and installation through Ash `FOR UPDATE`. The stable operation row serializes first outcome creation and replay; the installation row serializes provider-object identity reads/writes across distinct delivery operations without advisory SQL. Provider persistence, extensions, references, graph mapping, durable events, lifecycle changes, and the final outcome all commit in the snapshot action. A second outcome action owns retryable, terminal, revocation, storage-failure, and pre-operation transitions. | Reconciliation, product-mapping, concurrency, and webhook-worker suites prove ordering, exact replay, success/failure races, one provider identity, winning revocation, retry exhaustion, pre-operation terminalization, tenant scope, and rollback after every injected storage failure. The separate-owner GitHub integration suite proves newer provider state wins across distinct operations and concurrent extension-read failure leaves no partial provider graph before recovery. |
| `OfficeGraph.GitHubIntegration.OutboundWorker.persist_revoked_outcome/2` and `trace/2` | Reloads an outbound action before deciding whether a provider-reported revocation can terminalize it and revoke the installation, and reloads completed state before restoring missing audit/revision evidence. A stale worker must not revoke after a successful action wins, and trace recovery must not repeat provider access or duplicate evidence. | Separate transactional generic actions on `OutboundAction` lock the operation and exact action through Ash `FOR UPDATE`. The revocation action also locks the installation and changes its lifecycle only when the locked action is the terminal revocation winner. The trace action verifies the locked action's persisted terminal or successful state before idempotently ensuring both evidence records. Provider calls and durable Oban intent staging remain outside these transactions. | Outbound worker tests prove successful and terminal recovery, staged-intent retry, one audit/revision pair, trace-write rollback, winning revocation, installation-update rollback, and replay without repeated provider calls. The separate-owner GitHub integration suite proves concurrent provider revocations converge on one revoked installation and terminal actions. |
| `OfficeGraph.WorkGraph.ProposalCommands.create_signal/3`, `create_task/4`, `create_review_finding/4`, and `create_verification_check/4` | Reads and locks the exact operation plus any parent signal, task, or review finding before writing a document, graph item, typed node, relationship, audit, and revision. A stale or cross-scope parent must not produce partial graph state. | Each command dispatches to a transactional generic action on the node resource it creates. The action locks and revalidates the operation, actor session, expected action, capability, parent scope, and open parent lifecycle before dependent writes; all nested failures use the Ash data-layer rollback contract. | Proposed-change application, typed-flow authorization, WorkGraph persistence, document-failure rollback, and architecture suites prove exact scope, parent lifecycle, atomic graph assembly, and unchanged public errors. |
| `OfficeGraph.WorkGraph.RelationshipCommands.create/3`, `create_system/2`, `supersede/4`, `archive/4`, and `restore/4` | Reads endpoint, provenance, definition, operation, and existing relationship state before creating or changing an edge. Concurrent lifecycle or cycle decisions must not persist a partially superseded, archived, restored, or cyclic graph. | Transactional generic actions on `GraphRelationship` lock the operation and existing edge where present, revalidate scoped inputs and authorization, and use the declared active-edge identity for replay. Cycle-forbidden writes lock the definition row through Ash, which serializes distinct command operations, then perform a deterministic tenant- and definition-scoped breadth-first traversal over active relationships with an explicit 10,000-node safety bound. Edge, trace, and revision writes remain in the same action-owned transaction. | Relationship command, lifecycle, scope, replay, reciprocal-edge, distinct-operation cycle-concurrency, and persistence tests prove typed loser errors and atomic edge state. The separate-owner relationship suite also proves same-edge distinct-operation writes resolve to one active relationship identity. |
| `OfficeGraph.WorkGraph.VerificationCommands.complete_verification/4` and `satisfy_verification_check_from_evidence/3` | Locks the verification check's task, ordered findings, and ordered checks before deciding whether to create direct evidence or satisfy from accepted evidence and advance parent lifecycle. Concurrent completions must not duplicate results or stale-complete parents. | Transactional generic actions on `VerificationCheck` lock the operation and completion aggregate in parent-before-child order, revalidate authorization and required state, then atomically persist evidence or check state, parent transitions, audit, and revisions. All nested failures use the Ash data-layer rollback contract and public changeset errors are preserved. | Verification authorization, evidence acceptance, parent lifecycle, repeated-completion, concurrency, persistence rollback, and architecture suites prove one valid outcome and no partial graph. |
