# Production Read-Modify-Write Audit

Every production mutating function changed by this OpenSpec change must be
entered here before its persistence implementation changes. The audit records
the persisted reads that influence the write, the Ash concurrency safeguard
that owns the invariant, and the behavior or concurrency test that proves it.
Rows remain in this table after implementation so the final review can verify
that no touched invariant silently fell back to application-level
read-modify-write behavior.

| Owner and function | Persisted reads and invariant | Ash safeguard | Verification |
| --- | --- | --- | --- |
| `OfficeGraph.Tenancy.ensure_local_scope/1` | Reads or creates the organization by slug, then its workspace and initiative by parent-scoped slug, plus the default workstream. Exactly one row for each identity must survive concurrent first bootstrap attempts, and all returned records must belong to the same hierarchy. | One transactional generic action declares every touched tenancy resource. Each create uses an Ash upsert action backed by the existing identity and changes no fields on conflict. PostgreSQL 18 `MERGE` can surface a unique violation when two statements concurrently observe the same missing identity, so the boundary retries the complete action once only for the four declared tenancy identity constraints; the failed action has already rolled back before retry. | `OfficeGraph.Foundation.BootstrapTest` proves stable IDs on repeat bootstrap. `OfficeGraph.Integrations.IntakeBootstrapConcurrencyTest` proves two independent first-scope attempts return the same IDs and one row per identity. |
| `OfficeGraph.Content.persist_plain_document/3` | Performs no decision-making persisted read. The invariant is failure atomicity across the document, initial paragraph block, and first revision. | A transactional generic action on `Document` declares the block and revision resources it touches. Any nested Ash create error is returned to the action so Ash rolls back the full action-owned transaction. | `OfficeGraph.WorkGraph.PersistenceTest` proves the document, block, and revision are created as one domain operation and that invalid or unauthorized operation contexts persist none of them. |
| `OfficeGraph.Authorization.ensure_owner_role/2` | Ensures the complete recognized capability catalog, the organization owner role, every owner capability grant, the exact principal/workspace assignment, and policy bundle version 1. Concurrent owner bootstraps must converge without partial grants or duplicate assignments. | One transactional generic action on `Role` declares every authorization resource it touches. Identity-backed Ash upserts preserve existing rows. The boundary retries the complete rolled-back action once only for the declared authorization identity constraints when PostgreSQL 18 `MERGE` loses an initially-missing identity race. | `OfficeGraph.Foundation.BootstrapTest` proves repeat stability and exact grants. `OfficeGraph.Integrations.IntakeBootstrapConcurrencyTest` proves two independent first-owner attempts converge on one assignment and policy bundle. Authorization capability migration suites prove repeat bootstrap repairs missing grants. |
| `OfficeGraph.Authorization.persist_system_role/4` | Ensures the principal/scope-specific system role, requested capability rows, grants, and the nullable workspace assignment. Organization-wide and workspace assignments must remain distinct while repeated and concurrent setup converges. | A transactional generic action on `Role` uses the same identity-backed Ash upserts. `RoleAssignment.unique_assignment` uses `nils_distinct?: false`, so Ash matches nullable workspace identity without partial-index fragments. The same bounded full-action retry handles only declared authorization identity conflicts. | `OfficeGraph.Authorization.SystemRoleTest` proves organization/workspace separation, repeat setup, classified storage failure, and authorization. The owner-bootstrap concurrency suite covers the shared authorization identity race machinery. |
| `OfficeGraph.Identity.ensure_owner/1` | Ensures one principal by exact owner email and one profile by principal. Concurrent local-owner bootstrap must return the same records without leaving a principal without its profile. | A transactional generic action on `Principal` declares `PrincipalProfile` as touched and uses identity-backed create upserts. A single full-action retry is limited to the principal/profile identity constraints after a PostgreSQL 18 `MERGE` conflict has rolled the action back. | `OfficeGraph.Foundation.BootstrapTest` proves stable owner/profile IDs. `OfficeGraph.Integrations.IntakeBootstrapConcurrencyTest` forces two first-owner attempts and proves one principal/profile pair survives. |
| `OfficeGraph.Identity.ensure_session_context/3` | Ensures the one active `local_owner` session for a principal and exact organization/workspace while preserving revoked-session replacement behavior. | The `Session.unique_context` identity owns the partial active-session match; a dedicated Ash upsert action returns the existing active session. A bounded retry handles only its declared unique index race. | `OfficeGraph.Foundation.BootstrapTest` proves repeat session stability and replacement after revocation. The owner-bootstrap race test proves concurrent first session issuance converges. |
| `OfficeGraph.Identity.ensure_system_principal/2` | Ensures one principal per exact email, then verifies the stored kind and active status match the requested system identity. | The principal email identity owns the upsert; no get-then-insert window remains. The same bounded identity-conflict retry re-runs only the single Ash create action. | `OfficeGraph.Authorization.SystemRoleTest` and agent/GitHub integration tests prove repeat system-principal setup and kind/status rejection. |
| `OfficeGraph.Identity.store_oidc_login_transaction/1` | Deletes expired login guards and creates one new database-generated transaction ID in the same operation. Live guards must remain. | A transactional generic action reads expired guards and destroys them through Ash before creating the new guard. Any failure rolls back both pruning and creation. | `OfficeGraph.AuthenticationTest` proves expired guards are pruned while live and newly created guards remain. |
| `OfficeGraph.Identity.consume_oidc_login_transaction/1` | Reads the exact guard, decides whether it is unexpired, and consumes it exactly once. Concurrent callbacks must have one winner. | A transactional generic action performs an Ash `FOR UPDATE` read, destroys the locked row, and returns the validity boolean as a successful action result so expired-row deletion commits. The boundary maps `false` to the public invalid-transaction error after commit. | `OfficeGraph.AuthenticationTest` proves one of two concurrent callbacks succeeds, replay fails, and expired guards fail before provider exchange. |
| `OfficeGraph.Identity.ExternalIdentityReconciliation.reconcile_with_evidence/2` | Reads the provider-subject binding, every link for the normalized verified email, and every principal matching that normalized email before deciding whether to authenticate or persist review. A known subject must remain stable, competing subjects must not both become active, and review outcomes must commit. | One transactional generic action locks normalized-email principals and links plus the exact provider-subject row through Ash `FOR UPDATE` queries in deterministic row order. The normalized match uses Ash string expressions. A provider-subject identity-backed unique conflict retries the complete rolled-back action once. A typed action result represents authentication or committed review without using an error to escape the action transaction. | `OfficeGraph.Identity.HumanAuthenticationTest` proves exact-subject stability, normalized and ambiguous email behavior, conflicting-subject review isolation, lifecycle rejection, and persisted review evidence. The identity concurrency additions cover simultaneous provider-subject reconciliation. |
| `OfficeGraph.Identity.HumanSessions.issue/4` | Revalidates and locks the principal and external-identity link, then reads and replaces any active human-web session for the exact principal and tenant scope. Identity disablement must win before issuance, and session replacement plus login/revocation evidence must be failure-atomic. | A transactional generic action on `Session` first locks the principal row, which serializes all issuance for that principal including a first session with no row to lock. It then locks the exact identity link and active same-scope sessions through Ash `FOR UPDATE` queries before performing Ash updates and creates. A typed result preserves classified lifecycle rejection without treating it as a persistence failure. | `OfficeGraph.Identity.HumanAuthenticationTest` proves identity revalidation, one active replacement, exact event evidence, and storage-failure classification. The identity concurrency additions prove simultaneous same-scope issuance serializes to one active session. |
| `OfficeGraph.Identity.HumanSessions.revoke/2` | Reads the exact human-web session and conditionally revokes it. Repeated or concurrent logout must create at most one revocation transition and one logout event. | A transactional generic action locks the exact session row through Ash `FOR UPDATE`; the lock is held through the Ash update and authentication-event create. Missing, wrong-purpose, active, and already-revoked states are typed successful action outcomes, while persistence errors roll back the transition and event together. | `OfficeGraph.Identity.HumanAuthenticationTest` proves logout idempotency, revoked-session rejection, and exact bounded logout evidence. The identity concurrency additions cover simultaneous repeated logout. |
| `OfficeGraph.Integrations.ensure_provider_source/2` | Ensures one provider source for the exact kind/key identity and preserves the first stored source record on replay. | A dedicated Ash create action upserts on `ExternalSource.unique_kind_key`, updates no fields on conflict, and returns the skipped record. A bounded complete-action retry handles only the declared source unique constraint if PostgreSQL 18 `MERGE` loses an initially-missing identity race. | GitHub installation, webhook, reconciliation, and product-mapping suites prove stable provider-source identity and conflict classification. |
| `OfficeGraph.Integrations.archive_system_delivery/3` | Reads the exact source-scoped provider delivery before deciding whether to create or replay its immutable archive. Matching delivery content and scope must replay; changed content or operation scope must conflict. | A transactional generic action locks the existing `ExternalSource` row through Ash `FOR UPDATE`, then reads or creates the provider-delivery archive. The stable source row serializes even the first archive for a delivery identity, eliminating the partial-index fragment. A bounded retry handles only the declared provider-delivery constraint as defense against older non-locking callers. | GitHub webhook receipt and worker suites prove created/replayed status, immutable-content conflict, exact tenant scope, and provider delivery lookup. |
| `OfficeGraph.Integrations.submit_or_replay_manual_intake_command/3` and `record_manual_intake/3` | Reads the exact operation replay, canonical manual source, accepted normalized event, immutable archive content, and existing proposed-change set before returning replay, conflict, accepted, or duplicate. One command operation must have one intake result; one source/replay identity must have one accepted event; archive, event, proposals, durable event, and job must commit together. | One transactional generic action locks command operations through Ash `FOR UPDATE`; legacy non-command operations retain their independent duplicate semantics without an unnecessary aggregate lock. The source is created by an identity-backed Ash upsert, and `NormalizedIntakeEvent.accepted_replay_key` selects the single accepted winner without a partial-index fragment in application code. Nested owning Ash actions create the archive/event/proposals and durable delivery in the same transaction. A bounded complete-action retry is limited to the source and accepted-replay identities; after the losing action rolls back, the retry returns a duplicate or typed content conflict from the committed winner. | Manual-intake replay, durable-delivery, command replay, source race, content-conflict, proposed-change failure, and intake concurrency suites prove exact replay, one accepted winner, typed conflict, source convergence, and failure atomicity. |
| `OfficeGraph.ProposedChanges.create_for_manual_intake/4` | Locks and revalidates the accepted normalized event, then reads its proposal set before either returning the exact complete set or creating the four required proposal types. A partial proposal set must never commit or be treated as replay. | A transactional generic action on `ProposedGraphChange` locks the normalized event through Ash `FOR UPDATE`. The existing normalized-event/change-type identity remains the database backstop, while the locked parent serializes the absent-set decision and all four Ash creates. Typed rejected action results preserve exact validation errors without committing partial writes. | Proposed-change behavior tests prove exact event/operation/scope validation and idempotent complete-set replay; integration failure-atomicity proves failed creation leaves no partial set. Task 3.5 adds the independent-writer first-creation proof. |
| `OfficeGraph.ProposedChanges.apply_all/3` | Reloads and locks the exact ordered proposal IDs, validates tenant scope, required types, accepted-event ownership, pending or exact replay state, then creates the graph lineage and marks every proposal with its applied operation/resource. No graph truth or partial proposal lifecycle may survive a later failure. | A transactional generic action owns authorization decision recording, locked proposal reads, nested WorkGraph actions, and internal lifecycle updates. A typed action result treats validation and authorization denials as committed business outcomes so denial evidence survives, while unexpected persistence failures fail the action and roll back the full graph/proposal mutation. | `OfficeGraph.ProposedChangesTest` proves validation precedence, authorization evidence, apply-only execution, exact replay, graph lineage, and pending-state preservation. Task 3.5 adds separate-owner winner/loser and deterministic replay coverage. |
| `OfficeGraph.WorkPackets.create_packet/3` | Reads the exact operation replay, required verification checks, and source/check pairing before deciding whether to replay or atomically create the packet, first immutable version, ordered children, and current-version pointer. No incomplete packet contract may commit. | A transactional generic action on `WorkPacket` locks the operation through Ash `FOR UPDATE`, so absent-record creation and replay classification serialize on a stable parent. `WorkPacket.unique_operation` remains the database identity backstop; all nested Ash creates, bulk child creation, and the current-version update participate in the owning action transaction. | Work-packet contract tests prove typed validation, exact replay/conflict behavior, child ordering, and failure rollback. Task 3.5 adds the separate-owner first-creation proof. |
| `OfficeGraph.WorkPackets.create_version/4` | Locks and reloads the packet and its current version before checking the caller's expected version, source/check contract, existing operation replay, next version number, ordered children, and current-version pointer. Two writers must not both advance one packet from the same current version. | A transactional generic action on `WorkPacket` locks both the operation and the scoped packet through Ash `FOR UPDATE`. The packet row serializes version-number allocation and pointer advancement, while `WorkPacketVersion.unique_packet_version` backs the invariant. Any replay conflict, stale expected version, child failure, or pointer failure returns an action error and rolls back the full version contract. | Work-packet contract tests prove stale-version rejection, exact operation replay, ordered child reconstruction, and failure atomicity. Task 3.5 adds separate-owner same-version winner/loser coverage. |
