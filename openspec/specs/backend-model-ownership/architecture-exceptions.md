# Architecture Exception Ledger

This ledger records the remaining approved direct Ecto mutation paths after the
WorkGraph Ash repair. Normal walking-loop product resources should move through
Ash resources and domain actions. Entries here are explicit exceptions that keep
graph identity, relationship, bootstrap, intake, change-proposal, and
maintenance writes visible until a later change narrows or retires them.

| File | Owner | Approved functions | Allowed operation type | Approving spec | Reason | Retirement condition |
| --- | --- | --- | --- | --- | --- | --- |
| `lib/office_graph/work_graph/command_support.ex` | `OfficeGraph.WorkGraph` | `{transaction/1, Repo.transaction}` | Transaction boundary only | `repair-ash-model-conformance/backend-model-ownership` | Focused WorkGraph command modules share one explicit transaction boundary for graph identity, relationship, typed resource, lifecycle, and document writes so partially-created graph state is not committed. Table-backed WorkGraph records use Ash actions inside that boundary. | Retire or narrow this entry when WorkGraph creation and lifecycle completion are expressed as fully Ash-managed atomic workflows. |
| `lib/office_graph/integrations.ex` | `OfficeGraph.Integrations` | `{insert_manual_intake/4, Repo.transaction}`, `{submit_or_replay_manual_intake_command/3, Repo.transaction}`, `{insert_source_then_refetch/1, Repo.insert_all}` | Transaction boundary plus idempotent conflict insert | `complete-operator-command-loop/manual-intake-adapter` | Manual intake command replay locks the operation before choosing replay or creation, while intake persistence creates source, raw archive, normalized event, and proposals through Ash actions inside the same transaction. External source creation uses one `ON CONFLICT DO NOTHING` insert plus an Ash refetch so first concurrent intakes sharing a source do not poison the transaction. | Retire or narrow this entry when manual intake storage and command replay are expressed as a fully Ash-managed atomic workflow or Ash upsert handles these races without direct SQL. |
| `lib/office_graph/github_integration.ex` | `OfficeGraph.GitHubIntegration` | `{persist_binding/3, Repo.transaction}`, `{lock_installation!/1, Repo.query!}` | Authorized installation-binding transaction boundary and transaction-scoped advisory lock | `add-github-review-integration/github-review-integration` | Installation authority, backend principals, permission snapshot entries, credential metadata, and the current-snapshot pointer must commit atomically after the human command operation and tenant capability are validated. The advisory lock serializes global external-installation claims so concurrent tenants receive one stable winner and one non-enumerating rejection. Table-backed records are written through Ash actions inside the boundary. | Retire when installation binding is expressed as one policy-aware Ash-managed atomic command without losing replay or global provider-identity serialization. |
| `lib/office_graph/github_integration/webhook_receipt.ex` | `OfficeGraph.GitHubIntegration` | `{record_receipt/5, Repo.transaction}` | Verified provider-delivery transaction boundary only | `add-github-review-integration/github-review-integration` | After installation lookup, scoped credential resolution, constant-time signature verification, and event allowlisting, the receipt must atomically create or replay its system operation, immutable archive, typed event, dispatch job, and reconciliation job. | Retire when verified receipt and Oban enqueue can be expressed as one policy-aware Ash-managed atomic workflow without weakening pre-archive rejection. |
| `lib/office_graph/identity.ex` | `OfficeGraph.Identity` | `{ensure_owner/1, Repo.transaction}`, `{ensure_session_context/3, Repo.transaction}` | Local bootstrap transaction boundary only | `repair-ash-model-conformance/backend-model-ownership` | The walking skeleton needs a controlled local owner/principal/session bootstrap path before the full identity provider work exists. Identity bootstrap wraps dependent local records in transactions while the shared Repo helper owns idempotent conflict inserts and Ash refetches. | Replace or narrow this when authentik/Keycloak/SCIM identity flows are implemented. |
| `lib/office_graph/tenancy.ex` | `OfficeGraph.Tenancy` | `{ensure_local_scope/1, Repo.transaction}` | Local bootstrap transaction boundary only | `repair-ash-model-conformance/backend-model-ownership` | Development and test bootstrap needs first organization, workspace, initiative, and default workstream records before product flows can run. Tenancy bootstrap wraps dependent local records in a transaction while the shared Repo helper owns idempotent conflict inserts and Ash refetches. | Replace with a setup/onboarding domain when tenant provisioning is designed. |
| `lib/office_graph/authorization.ex` | `OfficeGraph.Authorization` | `{ensure_owner_role/2, Repo.transaction}`, `{persist_system_role/4, Repo.transaction}` | Atomic human-bootstrap and system-role transaction boundaries | `add-github-review-integration/shared-operation-contracts` | Owner bootstrap and restricted system-role grants each coordinate dependent role, capability, join, and assignment records. The explicit transaction prevents a partial capability grant from surviving a later insert failure while the shared Repo helper owns idempotent conflict inserts and Ash refetches. | Replace with policy-aware role-management actions that express each grant as one Ash-managed atomic workflow. |
| `lib/office_graph/durable_delivery.ex` | `OfficeGraph.DurableDelivery` | `{transaction/1, Repo.transaction}`, `{list_terminal_jobs/2, Repo.all}` | Transaction boundary plus bounded Oban infrastructure read | `add-durable-work-delivery/durable-work-delivery` | The durable-delivery boundary atomically creates a typed domain event and its unique Oban job when no owning product transaction is already active; callers already inside a transaction reuse that transaction. It also owns a scope-filtered read of Oban's infrastructure table and maps rows into safe summaries without raw arguments or errors. Table-backed domain events use Ash actions inside the boundary. | Retire the transaction entry when event and job creation can be expressed as one fully Ash-managed atomic workflow without losing Oban transaction coupling; retain the Oban read only while Oban remains the selected infrastructure owner. |
| `lib/office_graph/repo.ex` | `OfficeGraph.Repo` | `{insert_then_fetch!/5, Repo.insert_all}` | Shared idempotent conflict insert helper | `repair-ash-model-conformance/backend-model-ownership` | Local identity, tenancy, and authorization bootstrap paths share one `ON CONFLICT DO NOTHING` insert plus Ash refetch helper so first concurrent bootstraps do not poison the transaction on unique-key races. | Retire when Ash upsert handles these bootstrap races without direct SQL or when setup/onboarding workflows own bootstrap creation end to end. |
| `lib/office_graph/proposed_changes.ex` | `OfficeGraph.ProposedChanges` | `{create_for_manual_intake/4, Repo.transaction}`, `{apply_all/3, Repo.transaction}` | Transaction boundary only | `repair-ash-model-conformance/backend-model-ownership` | Manual intake creates the four pending change proposals through Ash actions inside one explicit transaction boundary, and change-proposal application locks pending rows, creates graph truth, and marks changes applied inside one explicit transaction so retry races cannot commit partial graph state. | Retire or narrow this entry when change-proposal intake and application are expressed as fully Ash-managed atomic workflows. |
| `lib/office_graph/content.ex` | `OfficeGraph.Content` | `{persist_plain_document/3, Repo.transaction}` | Transaction boundary only | `add-github-review-integration/github-review-integration` | Human and authenticated system document creation share one transaction that writes the document, first paragraph block, and initial revision through Ash actions after their distinct operation checks pass. | Retire or narrow this entry when Content creation is expressed as a fully Ash-managed atomic workflow. |
| `lib/office_graph/github_integration/reconciler.ex` | `OfficeGraph.GitHubIntegration` | `{reconcile_snapshot/5, Repo.transaction}`, `{record_failure/4, Repo.transaction}`, `{record_storage_failure/2, Repo.transaction}`, `{exhaust_retry/3, Repo.transaction}`, `{persist_pre_operation/2, Repo.transaction}`, `{lock!/1, Repo.query!}` | Authorized reconciliation, failure recording, and terminalization transactions with transaction-scoped advisory locks | `add-github-review-integration/github-review-integration` | One authenticated provider snapshot must atomically reconcile provider-neutral resources, GitHub extensions, external references, product signals, typed relationships, and its sync outcome. Provider failures, transient integration-record read failures, exhausted retryable work, and pre-operation delivery exhaustion must atomically lock and transition the same operation-scoped outcome so concurrent success, failure, and terminalization attempts cannot stale-overwrite one another. Stable advisory locks serialize provider identities and operation outcomes. | Retire when Ash-managed atomic workflows can preserve cross-domain reconciliation, provider-object serialization, operation-outcome serialization, storage-failure classification, and durable terminalization. |
| `lib/office_graph/github_integration/outbound_commands.ex` | `OfficeGraph.GitHubIntegration` | `{persist_and_enqueue/6, Repo.transaction}` | Authorized outbound-action and unique-job transaction boundary | `add-github-review-integration/github-review-integration` | A version- and permission-checked review reply or check update must atomically create one explicit action record, its unique worker job, audit trace, and revision trace after locking the command operation for replay. | Retire when an Ash-managed command can atomically persist outbound provenance and its Oban job with the same replay serialization. |
| `lib/office_graph/github_integration/outbound_worker.ex` | `OfficeGraph.GitHubIntegration` | `{trace/2, Repo.transaction}` | Durable outbound trace-recovery transaction boundary | `add-github-review-integration/github-review-integration` | A completed outbound action can be replayed after a transient trace-storage failure. The worker locks the owning operation and atomically ensures both the audit and revision records without reissuing the provider mutation, so a retry cannot commit only half of the required provenance. | Retire when an Ash-managed trace command can atomically ensure both provenance records while preserving operation serialization and provider-call idempotency. |
| `lib/office_graph/work_graph/system_commands.ex` | `OfficeGraph.WorkGraph` | `{transact/1, Repo.transaction}` | Authorized integration-signal transaction boundary only | `add-github-review-integration/github-review-integration` | The WorkGraph owner atomically ensures or refreshes the external-reference graph item, content document, canonical signal, typed relationship, audit, and revision after a scoped system operation is authenticated. | Retire when integration-signal synchronization is expressed as one policy-aware Ash-managed atomic workflow. |
| `lib/office_graph/work_packets.ex` | `OfficeGraph.WorkPackets` | `{create_packet_records/3, Repo.transaction}`, `{create_version_records/4, Repo.transaction}` | Transaction boundary only | `complete-operator-command-loop/work-packet-contracts` | Packet creation and versioning coordinate immutable versions with action-derived lifecycle, typed ordered source references, required-check references, concurrency guards, and the current-version pointer through Ash actions inside explicit transaction boundaries so partial packet contracts do not commit. | Retire or narrow this entry when packet creation and versioning are expressed as fully Ash-managed atomic workflows. |
| `lib/office_graph/work_graph/changes/validate_evidence_candidate_references.ex` | `OfficeGraph.WorkGraph` | `{validate_operation_context/4, Repo.query!}`, `{row_exists?/2, Repo.query!}` | Validation read only | `ash-api-surface` | Evidence candidate direct-create validation needs cross-context read probes against operations, run required checks, and execution observations without adding a WorkGraph-to-Runs module dependency cycle; the queries only validate supplied references and do not mutate state. | Retire or narrow this entry when evidence candidate reference validation can use an Ash-managed cross-context validation contract without circular dependencies. |
| `lib/office_graph/work_graph/relationship_cycle_policy.ex` | `OfficeGraph.WorkGraph` | `{lock_definition!/2, Repo.query!}`, `{reachable?/4, Repo.query!}` | Transaction-scoped advisory lock and bounded validation read | `implement-typed-graph-relationships/graph-relationships` | Typed relationship commands serialize forbidden-cycle checks by organization and definition, then use one bounded recursive query over active edges so concurrent contenders cannot both commit a cycle. | Replace with an Ash-managed recursive relationship constraint when it can preserve the same transaction-scoped serialization and explicit traversal bound. |
| `lib/office_graph/runs.ex` | `OfficeGraph.Runs` | `{create_run_records/4, Repo.transaction}`, `{create_observation/4, Repo.transaction}`, `{apply_accepted_verification_result/2, Repo.transaction}`, `{apply_required_check_result/3, Repo.transaction}`, `{lock_observation_idempotency_key!/2, Repo.query!}` | Transaction boundary plus transaction-scoped advisory lock | `complete-operator-command-loop/verification-evidence` | Work-run start and observation commands coordinate dependent Ash writes. Accepted evidence and governed waiver results flow through Verification, then narrow Runs lifecycle hooks lock the parent run and required-check rows before applying satisfied or waived state and recomputing aggregate verification. Observation replay uses a transaction-scoped advisory lock so concurrent same-key flows cannot commit partial packet/run state or surface database unique-key errors before one observation wins. | Retire or narrow this entry when work-run start, observation recording, verification result application, and observation-source replay serialization are expressed as fully Ash-managed atomic workflows. |
| `lib/office_graph/runs.ex` | `OfficeGraph.Runs` | `{projection_child_counts/1, Repo.query}` | Read-only aggregate projection support | `harden-project-quality/project-quality-gates` | The trusted run projection needs one bounded aggregate query for child counts; it uses caller-authorized run identifiers and returns counts only. | Retire when policy-aware aggregate relationships can provide the same bounded projection. |
| `lib/office_graph/projections/operator_workflow.ex` | `OfficeGraph.Projections` | `{relationship_details_page/3, Repo.query}`, `{read_relationship_counts_by_event_id/2, Repo.query}`, `{read_work_packet_required_checks/2, Repo.query}`, `{read_runs_for_workflow_events/2, Repo.query}` | Read-only bounded projection queries | `harden-project-quality/project-quality-gates` | The trusted operator projection uses scoped identifiers to page relationship detail and batch relationship, check, and run summaries without per-row query growth. | Replace with policy-aware generated aggregates and bounded relationships when they preserve the projection contract. |
| `lib/office_graph/projections/run_state.ex` | `OfficeGraph.Projections` | `{activity_page/3, Repo.query}`, `{command_option_page/4, Repo.query}`, `{read_command_option_summary/2, Repo.query}` | Read-only bounded run projection queries | `harden-project-quality/project-quality-gates` | The trusted run projection pages activity and command choices and computes a bounded option summary from an already-authorized run. | Replace with policy-aware generated connections and aggregates when they preserve stable cursor ordering. |
| `lib/office_graph/verification.ex` | `OfficeGraph.Verification` | `{create_evidence_candidate_record/3, Repo.transaction}`, `{accept_evidence_candidate_record/4, Repo.transaction}` | Transaction boundary only | `complete-operator-command-loop/verification-evidence` | Evidence candidate creation and acceptance lock their operation and target records, create typed evidence results through Ash actions, update the owning required check and run lifecycle, emit audit and revision provenance, and compare command replay facts inside explicit transaction boundaries so partial verification decisions do not commit. | Retire or narrow this entry when evidence candidate creation and acceptance are expressed as fully Ash-managed atomic workflows. |
| `lib/office_graph/verification/waiver.ex` | `OfficeGraph.Verification` | `{waive_required_check_record/4, Repo.transaction}` | Transaction boundary only | `complete-operator-command-loop/verification-evidence` | Governed waiver commands lock their operation and target records, create typed waiver results through Ash actions, update the owning required check and run lifecycle, emit audit and revision provenance, and compare command replay facts inside an explicit transaction boundary so partial verification decisions do not commit. | Retire this entry when governed waivers are expressed as a fully Ash-managed atomic workflow. |

## Authorization Bypass Ledger

This ledger records broad `authorize?: false` Ash call sites. These entries are
not approval for new bypasses; they make existing bypass scope explicit so
architecture conformance can reject untracked growth.

| File | Owner | Approved functions | Bypass scope | Approving spec | Reason | Verification coverage | Retirement condition |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `lib/office_graph/audit.ex` | `OfficeGraph.Audit` | `record!/5`, `record_once!/5`, `count_for_operation/1` | Trace writes, idempotent trace-recovery reads, and operation-scoped trace count reads | `stabilize-architecture-foundation/backend-model-ownership` | Audit records are internal trace records created after public commands have already authorized the product operation. Idempotent recovery reads use the exact operation, action, resource type, and resource ID while the caller holds the operation lock. | Architecture conformance plus workflow tests that assert audit traces and completed outbound replay without provider re-execution. | Replace with owner-scoped trace actions or policy-aware audit write and ensure commands. |
| `lib/office_graph/authorization.ex` | `OfficeGraph.Authorization` | `granted_capability?/3`, `record_decision/5`, `role_assignment_exists/4`, `role_ids_for_capability/2`, `role_ids_in_organization/2` | Policy lookup, decision trace, and human or system capability checks | `add-github-review-integration/shared-operation-contracts` | Capability evaluation runs before full authorization administration exists and must inspect policy rows directly inside the authorization boundary; system principals have no human session through which to perform those reads. | Foundation authorization tests, system-operation tests, and architecture conformance. | Replace with policy bundle and role-management actions with explicit human and system principal read policies. |
| `lib/office_graph/content.ex` | `OfficeGraph.Content` | `ash_create/2`, `plain_text_for_scope/2` | Internal document creation and scope-checked document text reads | `stabilize-architecture-foundation/backend-model-ownership` | Content commands create document, block, and revision records after caller authorization is checked by the owning workflow; human and system reads both pass an already-validated exact organization/workspace scope before the private lookup. | WorkGraph persistence, integration signal content refresh, and content trace tests. | Express document creation and reads through policy-aware Content commands. |
| `lib/office_graph/external_refs.ex` | `OfficeGraph.ExternalRefs` | `reference_by_lookup/2` | Provider-reference identity lookup after system-operation validation | `add-github-review-integration/github-review-integration` | The owning boundary resolves one organization, optional workspace, source, and external identity after authenticated reconciliation and rejects any provider or resource mismatch before updating safe projection fields. The lookup is also the read half of the boundary's conflict-safe get-or-insert contract for concurrent reconciliation. | Reconciliation ordering, replay, product mapping, concurrent identity creation, provider ownership, organization/workspace isolation, and architecture tests. | Replace with a policy-aware ExternalRefs upsert action that preserves tenant-scoped provider identity collision handling. |
| `lib/office_graph/identity.ex` | `OfficeGraph.Identity` | `active_principal?/1`, `active_system_principal/1`, `fetch_existing/2`, `validate_session_context/1` | Local owner bootstrap plus human-session and system-principal validation | `add-github-review-integration/shared-operation-contracts` | Local identity bootstrap and authenticated service or webhook operation creation need internal principal/session checks before external identity provider administration exists. | Foundation bootstrap tests, system-operation tests, and architecture conformance. | Replace with IdP-backed identity/session commands and scoped principal read policies. |
| `lib/office_graph/integrations.ex` | `OfficeGraph.Integrations` | `accepted_duplicate/2`, `ash_create/2`, `existing_intake_for_operation/2`, `fetch_provider_archive/2`, `fetch_source/1`, `get_or_create_source/1`, `read_provider_delivery_archive/2`, `verify_duplicate_content/3` | Manual intake command replay, source lookup, provider archive replay, raw archive verification, and normalized event writes | `add-github-review-integration/github-review-integration` | Integrations owns source replay identity and immutable raw archives; provider delivery lookup requires the exact organization, workspace, archive, and external delivery identities supplied by the verified worker contract. | Manual intake replay, provider delivery duplicate/conflict, webhook controller/worker, and architecture conformance tests. | Replace with Ash-managed intake and provider archive workflows with scoped integration read contracts. |
| `lib/office_graph/operations.ex` | `OfficeGraph.Operations` | `create_operation/5`, `create_system_operation/1`, `existing_operation/3`, `existing_system_operation/1`, `lock_operation/1`, `lock_scoped_target/3`, `read_operation/1` | Human and system operation creation, idempotency replay lookup, operation locking and recovery reads, and shared scoped command-target locking | `add-github-review-integration/shared-operation-contracts` | Operation correlation is infrastructure created only after human session or system-principal authorization. Fresh packet-version commands and run starts share one organization/workspace-scoped target lock after public command authorization, system operations use an exact organization, optional workspace, principal, action, and idempotency scope, and worker recovery performs a non-locking read before the reconciliation boundary acquires its transaction-scoped terminalization lock. | Concurrency, cross-workspace and organization-scoped system replay, human operation replay, packet run, stale-version, durable delivery, webhook recovery, and evidence acceptance tests. | Introduce policy-aware operation and scoped target-lock or recovery-read commands, or Ash-managed atomic workflows. |
| `lib/office_graph/github_integration.ex` | `OfficeGraph.GitHubIntegration` | `binding_result/2`, `credential_by_reference/2`, `read_installation/1` | Authorized installation replay, scoped credential reuse, and provider-identity uniqueness reads | `add-github-review-integration/github-review-integration` | The boundary authorizes the human operation first, then checks global provider identity without revealing conflicting tenant data, classifies lookup outages at the shared installation-read boundary, reuses only active credential metadata in the exact organization/workspace scope, and reconstructs only the caller-owned replay result. | Installation binding authorization and storage-failure classification, credential reuse, replay, cross-tenant rejection, API non-disclosure, and architecture conformance tests. | Replace with policy-aware installation and credential command reads that preserve global provider identity collision handling without tenant enumeration. |
| `lib/office_graph/github_integration/secret_store.ex` | `OfficeGraph.GitHubIntegration` | `load_credential/1` | Credential metadata lookup after explicit tenant scope input | `add-github-review-integration/github-review-integration` | Secret resolution is intentionally outside product resource policies; the narrow boundary loads one credential by opaque ID, verifies exact organization/workspace scope and active state, then delegates the reference to the configured secret adapter. | Secret store tenant-isolation, missing-reference, environment-adapter, and architecture conformance tests. | Replace with managed secret infrastructure exposing a policy-aware scoped credential resolution command. |
| `lib/office_graph/github_integration/reconciler.ex` | `OfficeGraph.GitHubIntegration` | `authorized_installation/2`, `base_by_extension/5`, `check_run_by_extension/3`, `check_run_extension!/3`, `extension_by_node!/3`, `missing_product_references!/5`, `outcome_by_operation/1`, `resolve_credential/2` | Authenticated reconciliation replay, installation authority, credential binding, tenant-scoped provider-extension identity reads, PR-qualified check-run identity reads, and authoritative-absence product-reference reads | `add-github-review-integration/github-review-integration` | Reconciliation first validates its system operation, then uses exact operation, installation, organization, workspace, principal, credential, provider-object, source, and pull-request identities. Check-run extension lookups additionally require the selected pull request so a provider check associated with multiple pull requests keeps independent projections. Authoritative collection reconciliation reads only active review-comment and check-run extensions plus their external references in that exact scope so missing provider work can close its mapped signal. These internal reads cannot be actor-policy reads because the service principal has no human session; mismatches return only safe non-enumerating errors. | Reconciliation ordering, replay, failure classification, multi-PR check projection, product mapping including authoritative absence, concurrency, tenant-isolation, secret scope, and architecture tests. | Replace with policy-aware system-principal read and provider-extension upsert or authoritative-collection actions that retain fail-closed scope checks. |
| `lib/office_graph/github_integration/outbound_commands.ex` | `OfficeGraph.GitHubIntegration` | `action_by_operation/1`, `active_installation/2`, `check_run_extension/1`, `require_installation_provenance/2`, `require_permission/2`, `review_comment_extension/1`, `scoped_target/3` | Authorized outbound target, permission, reconciliation-provenance, provider-extension, and command replay reads | `add-github-review-integration/github-review-integration` | The human command authorizes first, then exact organization/workspace, installation lifecycle, permission snapshot, selected-installation reconciliation provenance, target provider version, and GitHub extension identity checks fail closed before any credential or adapter access. | Outbound allowlist, replay, stale-version, installation provenance, permission, API, and architecture tests. | Replace with policy-aware outbound command actions and provider-extension relationships. |
| `lib/office_graph/github_integration/outbound_worker.ex` | `OfficeGraph.GitHubIntegration` | `action/3`, `active_installation/1`, `credential_binding/1`, `require_current_target_version/2`, `update_action/2` | Durable outbound action, installation, credential-binding, execution-time target-version reads, and result persistence | `add-github-review-integration/github-review-integration` | The worker accepts only the recorded action ID and exact tenant scope, rechecks installation lifecycle and the exact target provider version, resolves only its private-key binding, maps the recorded allowlisted action kind to one adapter callback, and persists the result on that same action after staging successful provider identity or terminal intent in durable job metadata. | Outbound worker success, successful-write recovery, delayed stale-version rejection, retry, terminal-write recovery, completed-trace recovery, credential, and architecture tests. | Replace with policy-aware system-principal outbound execution and result-recording actions. |
| `lib/office_graph/github_integration/health.ex` | `OfficeGraph.GitHubIntegration` | `scoped_installation/2`, `credentials/1`, `credential_records/1`, `failure_counts/2`, `last_success/1`, `permissions/1`, `recent_failure_actions/2`, `recent_failure_outcomes/2` | Bounded tenant-scoped integration health assembly | `add-github-review-integration/integration-health` | After projection authorization, health rechecks exact installation organization/workspace scope and performs a constant number of bounded reads over safe permission and credential status, filtered failure samples, complete aggregate failure counts, outbound failures, and the latest successful transition. It never reads secret references, action input, raw payloads, or job errors. | Integration health query-count, complete-count, transition-time, dependency-storage-failure, failure-limit, non-disclosure, cross-tenant, GraphQL/JSON, and architecture tests. | Replace with generated policy-aware aggregate projections when they preserve the same fixed query bound, complete headline counts, and safe field contract. |
| `lib/office_graph/github_integration/webhook_worker.ex` | `OfficeGraph.GitHubIntegration` | `load_installation/3`, `private_key_credential/1`, `receipt_operation/2` | Durable reconciliation installation, credential-binding, and exact receipt-operation reads | `add-github-review-integration/github-review-integration` | The worker accepts only the verified job envelope, rechecks exact installation organization/workspace lifecycle, resolves only its app-private-key binding, delegates scoped archive retrieval and secret resolution to their owning boundaries before provider access, and reloads only the exact accepted receipt operation before durable pre-operation terminalization. | Webhook worker reconciliation, credential failure, replay, exhausted pre-operation terminalization, and architecture tests. | Replace with policy-aware system-principal installation, credential, and receipt-operation read actions. |
| `lib/office_graph/github_integration/webhook_receipt.ex` | `OfficeGraph.GitHubIntegration` | `active_installation/1`, `webhook_credential/1` | Pre-archive active installation and credential-binding lookup | `add-github-review-integration/github-review-integration` | The webhook boundary must select one installation and its webhook credential before signature verification; it returns only safe error atoms and performs no archive, operation, event, or job write until both checks and constant-time verification pass. | Webhook invalid-signature, unknown-installation, unsupported-event, duplicate, controller, and architecture tests. | Replace with policy-aware authenticated installation lookup once provider request principals can be represented before signature verification. |
| `lib/office_graph/projections/operator_workflow.ex` | `OfficeGraph.Projections` | `graph_relationships_for_links/2`, `read_audit_records_by_operation_id/1`, `read_intake_event/2`, `read_intake_events/3`, `read_proposed_changes_by_event_id/2`, `read_revision_records_by_operation_id/1`, `read_scoped_many/3`, `read_work_packet_source_references/2` | Read-only operator workflow projection assembly | `stabilize-architecture-foundation/ui-projection-contracts` | Operator workflow reads manually assemble policy-filtered mixed graph, trace, proposal, packet, and run data while resource projection contracts are still being stabilized. Relationship assembly now receives the session explicitly so typed edge rows are constrained by organization and governing workspace. | Operator workflow projection and API parity tests. | Replace with policy-aware projection contracts and generated reads where safe. |
| `lib/office_graph/projections/packet_readiness.ex` | `OfficeGraph.Projections` | `read_graph_items/2`, `read_verification_checks/2` | Read-only packet readiness projection assembly | `stabilize-architecture-foundation/ui-projection-contracts` | Packet readiness reads validate source graph items and required checks before packet creation while resource projection contracts are still being stabilized. | Operator workflow projection and API parity tests. | Replace with policy-aware projection contracts and generated reads where safe. |
| `lib/office_graph/projections/packet_workspace.ex` | `OfficeGraph.Projections` | `count_versions/2`, `read_current_version/2`, `read_packet/2`, `read_required_checks/2`, `read_source_references/2`, `read_verification_checks/3`, `read_versions_page/4` | Read-only packet workspace projection assembly | `complete-operator-command-loop/packet-workspace` | Packet workspace reads assemble one already-authorized, organization/workspace-scoped packet contract across immutable versions, ordered child records, live required checks, readiness, and run-start affordance. | Packet workspace projection authorization tests and GraphQL API parity tests. | Replace with policy-aware resource relationships and generated reads plus a narrow derived readiness projection. |
| `lib/office_graph/projections/run_state.ex` | `OfficeGraph.Projections` | `read_evidence_candidates/3` | Read-only run state projection assembly | `stabilize-architecture-foundation/ui-projection-contracts` | Run state reads evidence candidates alongside run summaries while resource projection contracts are still being stabilized. | Operator workflow projection and API parity tests. | Replace with policy-aware projection contracts and generated reads where safe. |
| `lib/office_graph/proposed_changes.ex` | `OfficeGraph.ProposedChanges` | `ash_update_internal!/3`, `lock_normalized_event!/3`, `read_existing_for_normalized_event/2`, `validate_accepted_normalized_event/2` | Proposal intake, application, locking, and validation internals | `stabilize-architecture-foundation/backend-model-ownership` | Legacy proposed graph changes remain temporary compatibility data and are coordinated inside the ProposedChanges boundary. | ProposedChanges tests, walking skeleton API tests, and architecture conformance. | Retire with generic proposed graph changes or replace with typed ChangeProposal domain commands. |
| `lib/office_graph/proposed_changes/proposed_graph_change/originating_operation_actor.ex` | `OfficeGraph.ProposedChanges` | `fetch_operation/1` | Resource policy operation lookup | `stabilize-architecture-foundation/backend-model-ownership` | The private policy check validates proposal operation ownership without exposing the operation read publicly. | ProposedChanges resource and API tests. | Replace with typed proposal validation actions or delete with old proposed graph changes. |
| `lib/office_graph/proposed_changes/proposed_graph_change/trace_reference_scope.ex` | `OfficeGraph.ProposedChanges` | `fetch_reference/2` | Resource validation reads | `stabilize-architecture-foundation/backend-model-ownership` | The private change validates operation and normalized-event trace references without exposing those reads publicly. | ProposedChanges resource and API tests. | Replace with typed proposal validation actions or delete with old proposed graph changes. |
| `lib/office_graph/proposed_changes/proposed_graph_change/validate_pending_update.ex` | `OfficeGraph.ProposedChanges` | `validate_current_status/2` | Internal status validation | `stabilize-architecture-foundation/backend-model-ownership` | The private change locks and validates the current proposal state before mutation. | ProposedChanges resource and API tests. | Replace with typed proposal validation actions or delete with old proposed graph changes. |
| `lib/office_graph/proposed_changes/proposed_graph_change/validate_unique_normalized_event_change_type.ex` | `OfficeGraph.ProposedChanges` | `validate_unique_event_change_type/3` | Internal uniqueness validation | `stabilize-architecture-foundation/backend-model-ownership` | The private change checks proposal uniqueness before create while the database identity remains authoritative under races. | ProposedChanges resource and API tests. | Replace with typed proposal validation actions or delete with old proposed graph changes. |
| `lib/office_graph/revisions.ex` | `OfficeGraph.Revisions` | `record!/5`, `record_once!/5`, `count_for_operation/1` | Revision trace writes, idempotent trace-recovery reads, and operation-scoped counts | `stabilize-architecture-foundation/backend-model-ownership` | Revisions are internal trace records created after public commands have authorized the product operation. Idempotent recovery reads use the exact operation, resource type, resource ID, and revision type while the caller holds the operation lock. | Architecture conformance plus workflow tests that assert revision traces and completed outbound replay without provider re-execution. | Replace with owner-scoped revision actions or policy-aware revision write and ensure commands. |
| `lib/office_graph/runs.ex` | `OfficeGraph.Runs` | `active_run_for_packet_version/2`, `existing_observation/2`, `existing_observation_for_operation/2`, `existing_run_result/4`, `failed_observations_for_run?/1`, `fetch_scoped/3`, `lock_required_checks_for_run!/1`, `lock_run!/1`, `lock_scoped_run!/2`, `mark_required_check_in_locked_run!/3`, `packet_required_checks/1`, `read_run_children/2`, `read_run_required_checks/2`, `required_checks_for_run/1`, `same_observation_replay?/3`, `set_run_verification_failed!/1`, `set_run_verified!/1`, `update_run_after_observation!/2`, `update_run_failed!/1`, `validate_required_check_contract/2` | Run creation, active-run conflict detection, observation replay, accepted or waived result lifecycle application, packet-contract validation, lock reads, and summary reads | `complete-operator-command-loop/verification-evidence` | Runs owns replay checks, packet-serialized active-run conflict detection, observation replay, required-check packet-contract validation, and aggregate lifecycle mutation after caller authorization while resource actions validate run-start and child-state transitions. Verification owns accepted evidence and waiver rules and calls Runs through narrow lifecycle hooks. | Work packet run verification, duplicate active-run, waiver, concurrency, operator workflow, and Runs action-contract tests. | Express run start, observation recording, verification result application, and recomputation as policy-aware domain commands. |
| `lib/office_graph/runs/changes/scoped_read.ex` | `OfficeGraph.Runs` | `fetch_with_lock/7` | Shared private scoped validation read | `stabilize-architecture-foundation/backend-model-ownership` | Run change modules share scoped read helpers to validate supplied run and packet-version references without exposing those reads publicly; run start locks the owning packet before proving that the requested version is still current. | Runs action-contract conformance and packet-run tests. | Replace with Ash relationship-backed validation or fully Ash-managed run-start and observation workflows. |
| `lib/office_graph/runs/changes/validate_run_start_contract.ex` | `OfficeGraph.Runs` | `packet_required_checks/1`, `packet_source_graph_item_ids/1`, `read_verification_checks/2` | Private run-start validation reads | `stabilize-architecture-foundation/backend-model-ownership` | Run create validation checks selected packet version scope, packet ownership, readiness child links, source/check pairing, and authority posture inside the private resource action before a run can start. | Runs action-contract conformance and packet-run tests. | Replace with Ash relationship-backed validation or fully Ash-managed run-start workflow. |
| `lib/office_graph/runs/changes/validate_observation_run_references.ex` | `OfficeGraph.Runs` | `fetch_verification_check/1`, `packet_source_graph_item?/2`, `required_check_graph_item?/2`, `run_requires_check?/2` | Private change validation reads | `stabilize-architecture-foundation/backend-model-ownership` | Observation create validation checks run, packet, source, and verification references inside private resource changes. | Runs resource validation tests and packet-run tests. | Replace with Ash relationships or domain validation contracts. |
| `lib/office_graph/runs/changes/validate_run_required_check_contract.ex` | `OfficeGraph.Runs` | `read_runs/1`, `read_packet_required_checks/3` | Batched private required-check contract validation reads | `eliminate-backend-query-fanout/backend-query-efficiency` | Required-check creation validates all scoped run and packet-version contracts with at most one Ash read per source inside the private batch resource change. | Runs action-contract query-count regressions and packet-run tests. | Replace with Ash relationship-backed validation or a policy-aware domain validation contract. |
| `lib/office_graph/repo.ex` | `OfficeGraph.Repo` | `ash_bulk_create!/2`, `ash_create!/2`, `ash_update!/1`, `fetch_by_lookup/2` | Shared internal Ash writes, ordered bulk child creation, and bootstrap lookup | `eliminate-backend-query-fanout/backend-query-efficiency` | Domain commands call these helpers after their public authorization or local bootstrap checks so shared internal Ash writes, ordered bulk child creation, and bootstrap refetches stay centralized and ledgered. | Architecture conformance plus the domain tests that exercise Runs, Verification, WorkPackets, Identity, Tenancy, and Authorization flows, including bulk rollback and result-order regressions. | Replace with policy-aware command actions or Ash-managed onboarding and child-creation workflows that no longer need broad internal bypass helpers. |
| `lib/office_graph/verification.ex` | `OfficeGraph.Verification` | `accept_locked_candidate!/4`, `existing_acceptance_for_operation/2`, `existing_candidate_for_operation/2`, `lock_candidate!/1`, `preflight_result_slot!/2`, `read_verification_result_for_evidence/1` | Evidence candidate creation, acceptance, replay, locking, result-slot conflict preflight, and verification result reads | `complete-operator-command-loop/verification-evidence` | Verification owns evidence acceptance, replay, target locking, result-slot conflict preflight, and recomputation internals while these workflows remain transaction-heavy. | Verification, packet-run, and concurrency tests. | Express evidence suggestion, acceptance, and recomputation as policy-aware Verification commands. |
| `lib/office_graph/verification/command_support.ex` | `OfficeGraph.Verification` | `fetch_scoped/3`, `lock_scoped!/3` | Shared scoped reads and target locking for verification commands | `complete-operator-command-loop/verification-evidence` | Verification command support centralizes already-authorized scoped reads and target locks shared by evidence and waiver workflows. | Verification, waiver, packet-run, and concurrency tests. | Replace shared bypass helpers with policy-aware scoped Verification reads. |
| `lib/office_graph/verification/waiver.ex` | `OfficeGraph.Verification` | `existing_waiver_for_operation/2`, `lock_run_required_checks!/1` | Governed waiver replay and required-check locking | `complete-operator-command-loop/verification-evidence` | Verification waiver execution owns governed replay and required-check locking while the workflow remains transaction-heavy. | Waiver, packet-run, and concurrency tests. | Express governed waivers as a policy-aware Verification command. |
| `lib/office_graph/work_graph/command_support.ex` | `OfficeGraph.WorkGraph` | `ash_create_internal/3`, `ash_get/2`, `ash_get_for_update/2`, `ash_read_one_internal/1`, `ash_update_internal/3` | Graph resource creation, internal lifecycle updates, replay reads, and scoped lock reads | `implement-typed-graph-relationships/graph-relationships` | WorkGraph owns typed graph truth writes, lifecycle updates, replay reads, and locks inside explicit authorized relationship transactions while command modules are split. | WorkGraph relationship command, replay, lifecycle, persistence, authorization, and architecture tests. | Express graph creation, replay, and lifecycle transitions as fully Ash-managed WorkGraph commands. |
| `lib/office_graph/work_graph/system_commands.ex` | `OfficeGraph.WorkGraph` | `active_reference_relationship/2`, `ensure_reference_item!/3`, `fetch_reference_item!/2`, `reference_item_for_update!/2`, `signal_by_graph_item/1` | System-operation-scoped product mapping replay and canonical-identity reads | `add-github-review-integration/github-review-integration` | After validating the integration service operation and exact organization/workspace scope, WorkGraph resolves and locks one persisted external-reference graph item plus its active typed edge so concurrent provider replay returns the canonical item and existing signal instead of duplicating product work. The same scoped lock/read path lets reconciliation refresh, close, or reopen the canonical signal as provider content and actionability change without creating placeholder work for first-seen inactive items. | Product mapping replay, signal content refresh, signal lifecycle, signal concurrency, reconciliation concurrency, typed relationship, and architecture tests. | Replace with a policy-aware atomic integration-signal command and relationship-backed lookup. |
| `lib/office_graph/work_graph/queries.ex` | `OfficeGraph.WorkGraph` | `authorized_item/2`, `batch_authorized_endpoints/2`, `read_adjacency/3`, `read_relationship/2` | Authorization-filtered relationship projection reads | `implement-typed-graph-relationships/graph-relationships` | Relationship adjacency and Relay node refetch perform one trusted capability check, then use explicit organization/workspace filters to read the relationship anchor and all visible endpoints in bounded batches; omitted endpoints are redacted rather than leaked. | Relationship query-count, endpoint-redaction, GraphQL, JSON, Relay-node, and architecture tests. | Replace with a generated policy-aware adjacency read when it preserves bounded batching and endpoint-level redaction. |
| `lib/office_graph/work_graph/verification_commands.ex` | `OfficeGraph.WorkGraph` | `lock_review_findings_for_task!/1`, `lock_verification_checks_for_findings!/1` | Parent-child lock reads for verification completion | `stabilize-architecture-foundation/backend-model-ownership` | Verification completion owns parent-before-child lock ordering while recomputing review finding and task lifecycle state inside one graph transaction. | WorkGraph persistence, authorization, and architecture tests. | Express verification completion and lifecycle recomputation as fully Ash-managed WorkGraph commands. |
| `lib/office_graph/work_graph/changes/validate_relationship_endpoints.ex` | `OfficeGraph.WorkGraph` | `fetch_graph_items/1` | Private graph relationship endpoint validation reads | `stabilize-architecture-foundation/backend-model-ownership` | Relationship create validation checks graph item endpoints inside a private resource change. | WorkGraph validation and persistence tests. | Replace with Ash relationships or domain validation contracts. |
| `lib/office_graph/work_graph/changes/validate_same_scope_references.ex` | `OfficeGraph.WorkGraph` | `fetch_references/2` | Batched private same-scope reference validation reads | `eliminate-backend-query-fanout/backend-query-efficiency` | WorkGraph batch create actions validate all referenced records with one scoped Ash read per resource without exposing broad cross-domain reads. | Architecture same-scope query-count and validation-equivalence tests. | Replace with modeled Ash relationships and policy-aware domain read contracts. |
| `lib/office_graph/work_graph/review_finding.ex` | `OfficeGraph.WorkGraph` | `validate_open_task/2` | Private parent-state validation read | `stabilize-architecture-foundation/backend-model-ownership` | Review finding creation checks parent task state inside the WorkGraph boundary. | WorkGraph persistence tests. | Replace with Ash relationship and invariant change owned by WorkGraph. |
| `lib/office_graph/work_graph/verification_check.ex` | `OfficeGraph.WorkGraph` | `validate_open_review_finding/2` | Private parent-state validation read | `stabilize-architecture-foundation/backend-model-ownership` | Verification check creation checks parent review finding state inside the WorkGraph boundary. | WorkGraph persistence tests. | Replace with Ash relationship and invariant change owned by WorkGraph. |
| `lib/office_graph/work_graph/verification_result/validate_result_evidence.ex` | `OfficeGraph.WorkGraph` | `fetch_evidence_item/1` | Private evidence result validation read | `complete-operator-command-loop/verification-evidence` | Verification result creation requires matching accepted evidence for passed or failed results and forbids fabricated evidence for governed waiver results. | Verification, waiver, and WorkGraph tests. | Move result recording ownership fully to Verification commands and replace the internal read with relationship-backed validation. |
| `lib/office_graph/work_packets.ex` | `OfficeGraph.WorkPackets` | `existing_packet_result/2`, `existing_version_result/3`, `read_current_version/1`, `read_version_for_operation/2`, `read_required_checks/1`, `read_required_verification_checks/2`, `read_source_references/1` | Packet command idempotency replay, current and operation-created version, and packet contract reads | `complete-operator-command-loop/work-packet-contracts` | WorkPackets owns replay comparison, immutable operation-created version reads, current-version reads, ordered source/check replay reads, required-check state validation, source/check pairing validation, and command-level transaction orchestration while shared Operations support owns the scoped packet-row lock and resource actions derive packet state and child defaults. | Work packet verification, delayed create replay, stale-version, replay, and architecture conformance tests. | Express packet creation and versioning transactions and replay reads as policy-aware WorkPackets commands or Ash-managed atomic workflows. |
| `lib/office_graph/work_packets/changes/validate_current_version.ex` | `OfficeGraph.WorkPackets` | `fetch_current_version/1` | Private current-version validation read | `stabilize-architecture-foundation/backend-model-ownership` | WorkPacket current-version updates must prove the selected version belongs to the packet and target scope inside the private Ash update action before the packet pointer changes. | WorkPackets action-contract tests and architecture conformance. | Replace with an Ash relationship-backed validation or fully Ash-managed packet-version lifecycle command. |
