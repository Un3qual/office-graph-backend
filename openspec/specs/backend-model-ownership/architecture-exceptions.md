# Architecture Debt And Authorization Bypass Ledger

## Historical Direct Database Debt Context

The first table preserves the rationale previously recorded for direct database
paths. Every row is unapproved removal debt under the current boundary; neither
its presence here nor its historical source spec grants permission to retain,
move, rewrite, or broaden the occurrence. The machine-readable
`openspec/specs/ecto-sql-boundaries/database-access-debt.json` inventory is the
authoritative current baseline, and
`openspec/specs/ecto-sql-boundaries/approved-database-exceptions.json` is the
only repository inventory of exact user-approved raw-SQL occurrences.

| File | Owner | Recorded operations | Operation type | Historical source spec | Removal rationale | Retirement condition |
| --- | --- | --- | --- | --- | --- | --- |
| `lib/office_graph/durable_delivery.ex` | `OfficeGraph.DurableDelivery` | `{transaction/1, Repo.transaction}`, `{list_terminal_jobs/2, Repo.all}` | Transaction boundary plus bounded Oban infrastructure read | `add-durable-work-delivery/durable-work-delivery` | The durable-delivery boundary atomically creates a typed domain event and its unique Oban job when no owning product transaction is already active; callers already inside a transaction reuse that transaction. It also owns a scope-filtered read of Oban's infrastructure table and maps rows into safe summaries without raw arguments or errors. Table-backed domain events use Ash actions inside the boundary. | Retire the transaction entry when event and job creation can be expressed as one fully Ash-managed atomic workflow without losing Oban transaction coupling; retain the Oban read only while Oban remains the selected infrastructure owner. |
| `lib/office_graph/repo.ex` | `OfficeGraph.Repo` | `{insert_then_fetch!/5, Repo.insert_all}` | Shared idempotent conflict insert helper | `repair-ash-model-conformance/backend-model-ownership` | Local identity, tenancy, and authorization bootstrap paths share one `ON CONFLICT DO NOTHING` insert plus Ash refetch helper so first concurrent bootstraps do not poison the transaction on unique-key races. | Retire when Ash upsert handles these bootstrap races without direct SQL or when setup/onboarding workflows own bootstrap creation end to end. |
| `lib/office_graph/work_graph/commands/system_commands.ex` | `OfficeGraph.WorkGraph` | `{transact/1, Repo.transaction}` | Authorized integration-signal transaction boundary only | `add-github-review-integration/github-review-integration` | The WorkGraph owner atomically ensures or refreshes the external-reference graph item, content document, canonical signal, typed relationship, audit, and revision after a scoped system operation is authenticated. | Retire when integration-signal synchronization is expressed as one policy-aware Ash-managed atomic workflow. |

## Authorization Bypass Ledger

This ledger records broad `authorize?: false` Ash call sites. These entries are
not approval for new bypasses; they make existing bypass scope explicit so
architecture conformance can reject untracked growth.

| File | Owner | Approved functions | Bypass scope | Approving spec | Reason | Verification coverage | Retirement condition |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `lib/office_graph/audit.ex` | `OfficeGraph.Audit` | `record!/5`, `record_once!/5`, `count_for_operation/1` | Trace writes, idempotent trace-recovery reads, and operation-scoped trace count reads | `stabilize-architecture-foundation/backend-model-ownership` | Audit records are internal trace records created after public commands have already authorized the product operation. Idempotent recovery reads use the exact operation, action, resource type, and resource ID while the caller holds the operation lock. | Architecture conformance plus workflow tests that assert audit traces and completed outbound replay without provider re-execution. | Replace with owner-scoped trace actions or policy-aware audit write and ensure commands. |
| `lib/office_graph/authorization.ex` | `OfficeGraph.Authorization` | `granted_capability_for_principal/4`, `record_decision/5`, `role_assignment_exists/4`, `role_ids_for_capability/2`, `role_ids_in_organization/2`, `run_role_action/2` | Policy lookup, decision trace, human or system capability checks, and internal role command dispatch | `remove-direct-database-access` | Capability evaluation runs before full authorization administration exists and must inspect policy rows directly inside the authorization boundary; bootstrap and system-role commands have no fully-authorized actor until their owning transactional role action finishes. | Foundation authorization, role bootstrap, system-operation, and architecture conformance tests. | Replace the remaining internal reads and bootstrap action dispatch with policy-aware role-management actions that can authorize both human and system principals directly. |
| `lib/office_graph/authorization/role.ex` | `OfficeGraph.Authorization` | `ensure/2` | Nested identity-backed role dependency creation | `remove-direct-database-access` | The owning role action has already validated its command envelope and creates exact capability, join, and assignment identities inside one transaction before an actor can possess the role being established. | Owner-role and system-role replay, rollback, uniqueness, and architecture conformance tests. | Give the nested role dependencies policies that accept the owning action context without a broad bypass. |
| `lib/office_graph/authorization.ex` | `OfficeGraph.Authorization` | `active_policy_bundle/1` | Active policy-bundle resolution for immutable invocation-authority capture | `implement-internal-agent-runtime/agent-invocation` | Invocation snapshots need the current policy identity and version after the agent's scoped grants have authorized. The lookup is constrained to one organization and the active lifecycle state and returns no policy rules or tenant data on absence. | Agent authority snapshot, tenant-isolation, and architecture tests. | Replace with a policy-aware policy-bundle resolution action for authenticated human and system principals. |
| `lib/office_graph/authorization.ex` | `OfficeGraph.Authorization` | `resolve_login_scope/2` | Current human role-assignment scope selection during OIDC login | `add-human-session-foundation/human-authentication` | After validated external identity reconciliation, login must derive organization and workspace only from current internal role assignments. The read is constrained to the exact principal, returns only distinct assigned scopes, and requires an explicit configured preference when more than one scope exists. | Authentication scope-selection, no-scope, preferred-scope, deduplication, and architecture tests. | Replace with a policy-aware human login-scope action or interactive scope selector backed by an authorized assignment relationship. |
| `lib/office_graph/agent_runtime.ex` | `OfficeGraph.AgentRuntime` | `binding_for_scope/3`, `canonical_definition/0`, `create_binding/3`, `persist_binding/2`, `replay_binding/4` | Action-owned canonical-definition locking, binding identity upsert, internal command dispatch, and backend-principal replay lookup | `remove-direct-database-access` | The public owner command authorizes the exact operation and scope before dispatching one transactional binding action. That action locks the canonical definition, constrains the binding lookup/upsert to its declared organization/workspace identity, and resolves only the exact active agent principal before ensuring its scoped role. | Agent runtime workspace-binding authorization, replay, tenant-isolation, inactive-definition, rollback, identity, and architecture conformance tests. | Give the binding action and nested principal/role dependencies policy-aware action contexts without broad bypasses. |
| `lib/office_graph/agent_runtime/values/authority.ex` | `OfficeGraph.AgentRuntime` | `context_expansion_lineage/1`, `credential/3`, `get/3`, `load_snapshot/1`, `validate_approval/3`, `validate_context_expansion/3` | Invocation-time credential resolution and pre-step authority-snapshot, execution, binding, definition, approval, and bounded expansion revalidation | `implement-internal-agent-runtime/agent-invocation` | Invocation computes and revalidates authority only after a scoped human or system command. Every lookup is bound to the persisted execution identity or exact organization/workspace scope; credential, approval, and every expansion record in the execution's linear immutable context-package lineage must be active and match the current execution step and active state lineage, snapshot integrity including the captured model-adapter identity is recomputed, and any missing or mismatched authority fails closed without returning foreign records. | Agent authority snapshot capability, adapter identity, credential, principal, grant, tool, approval, complete context-expansion lineage, retry, tenant-isolation, and architecture tests. | Replace with policy-aware AgentRuntime authority snapshot and step-authorization actions that preserve immutable capture and fail-closed revalidation. |
| `lib/office_graph/agent_runtime/services/context_assembler.ex` | `OfficeGraph.AgentRuntime` | `load_entries/1`, `load_package/1` | Immutable invocation-context replay by execution and package identity | `implement-internal-agent-runtime/agent-invocation` | Replay occurs only while the invocation command holds the operation, binding, definition, and invocation-identity locks and has matched all immutable execution facts. It loads only version one of the exact execution package and that package's ordered entries, including their selected source versions, so retries and bounded expansions cannot rebuild context fingerprints from changed source data. | Agent context immutable replay, source-version preservation, expansion, idempotency, and architecture tests. | Replace with policy-aware AgentRuntime package relationships or a replay action that preserves exact immutable version loading. |
| `lib/office_graph/agent_runtime/commands/invocation_commands.ex` | `OfficeGraph.AgentRuntime` | `create_invocation/6`, `create_invocation_dependencies/4`, `execution_by_invocation_identity/3`, `execution_by_operation/1`, `lock_record/2`, `persist/5`, `replay_invocation/6`, `scoped_binding/3` | Action-owned invocation dispatch, exact binding/definition locks, identity upsert, dependent immutable writes, and replay reads | `remove-direct-database-access` | Human and system entry points validate the exact operation envelope, authority, tenant scope, active binding, and run subject before dispatching one transactional invocation action. The action re-locks and compares the typed operation, binding, and definition; upserts only the declared binding/run/idempotency identity; and creates or replays the exact execution, authority snapshot, initial context, and worker operation. | Human and automatic invocation, immutable replay, inactive binding/definition, wrong operation/subject, tenant-isolation, rollback, identity, and architecture tests. | Give the invocation action and nested snapshot/context dependencies policy-aware action contexts without broad bypasses. |
| `lib/office_graph/agent_runtime/commands/cancellation_commands.ex` | `OfficeGraph.AgentRuntime` | `cancel_model_request/2`, `cancel_pending_gate/5`, `cancellation_replay?/2`, `lock_active_model_request/2`, `lock_execution/2`, `lock_model_request/2`, `lock_pending_gate/4`, `persist_cancel/4`, `persist_cancellation/4` | Action-owned cancellation dispatch, target locks, optimistic transitions, durable replay lookup, and post-commit adapter request loading | `remove-direct-database-access` | The public command validates the exact operation, capability, tenant scope, and expected execution version before dispatching one transactional execution action. The action locks pending gates before the execution and active request, performs Ash updates under the execution optimistic lock, records durable invalidations, and returns the immutable adapter request needed for signaling only after commit. | Queued, waiting, and running cancellation, gate retirement, lost-response replay, active adapter signaling, stale-version, tenant-scope, capability, rollback, and architecture conformance tests. | Give the cancellation action and nested gate, request, and event operations policy-aware action contexts without broad bypasses. |
| `lib/office_graph/agent_runtime/commands/approval_commands.ex` | `OfficeGraph.AgentRuntime` | `cancel_active_model_request/4`, `lock_execution/2`, `lock_request/2`, `persist_decision/6`, `persist_resolution/6` | Action-owned approval dispatch, exact request/execution locks, optimistic execution transition, and active-request cancellation | `remove-direct-database-access` | The public command validates the human operation, capability, tenant scope, and decision envelope before dispatching one transactional approval action. The action locks the exact operation, request, execution, and active model request; validates the captured waiting version; and atomically records the decision, execution transition, traces, events, and approved resume job. | Approval approve, deny, cancel, expiry, stale-version, replay, exact-resume, transport, rollback, and architecture tests. | Give the approval action and nested execution, model-request, trace, and delivery operations policy-aware action contexts without broad bypasses. |
| `lib/office_graph/agent_runtime/commands/context_expansion_commands.ex` | `OfficeGraph.AgentRuntime` | `expanded_package/1`, `persist_decision/8`, `persist_resolution/6`, `read_one/1` | Action-owned expansion dispatch, exact aggregate locks, immutable package creation, optimistic execution transition, and replay lookup | `remove-direct-database-access` | The public command validates the human operation, capability, tenant scope, and decision envelope before dispatching one transactional expansion action. The action locks the exact request, execution, authority snapshot, and current package; validates the captured waiting version and target scope; and atomically records the decision, successor package, execution transition, traces, events, and approved resume job. | Expansion approve, deny, cancel, expiry, stale-version, immutable-package replay, exact-resume, transport, rollback, and architecture tests. | Give the expansion action and nested package, execution, trace, and delivery operations policy-aware action contexts without broad bypasses. |
| `lib/office_graph/agent_runtime/workers/execution_worker.ex` | `OfficeGraph.AgentRuntime` | `authority_snapshot/1`, `claim/4`, `claim_available_step/6`, `claim_dispatch_posture/3`, `complete/3`, `complete_current_step/5`, `context_package/1`, `context_requires_expansion?/1`, `create_or_load_approval_request/4`, `create_or_load_context_expansion_request/4`, `create_or_load_request/4`, `existing_request_id/2`, `fail_unclaimed_step/3`, `finalize_current_step/6`, `finalize_step/5`, `load_definition/1`, `load_execution/1`, `lock_execution/1`, `lock_model_request/1`, `maybe_cancel_request/2`, `model_request/3`, `snapshot_credential_kinds/2`, `transition/4` | Action-owned worker dispatch, system-operation-scoped immutable context reads, lease-aware execution/request locks, gate/request creation, optimistic transitions, and bounded post-claim posture reads | `remove-direct-database-access` | The worker binds job arguments to one exact tenant and immutable snapshot/context/adapter contract, revalidates mutable authority, then dispatches typed claim, completion, pre-claim failure, and finalization actions. Those actions lock the operation, execution, and model request in stable order; preserve request identities; apply Ash optimistic execution transitions; and atomically persist gates, routed outputs, durable events, and retry or terminal outcomes. The adapter call remains outside the action, and the post-claim posture uses exact typed execution/request reads before dispatch. | Full execution-worker lifecycle, gate, retry, lease recovery, duplicate dispatch, cancellation, adapter provenance, routed-output rollback, authority drift, tenant isolation, and architecture conformance tests. | Give worker actions and their nested gate, request, output-owner, and delivery operations policy-aware system action contexts without broad bypasses. |
| `lib/office_graph/agent_runtime/workers/gate_expiry_worker.ex` | `OfficeGraph.AgentRuntime` | `expire/2`, `fail_waiting_execution/4`, `lock/2`, `mark_expired/3`, `mark_superseded/2` | Action-owned gate-expiry dispatch, exact request/execution locks, optimistic execution failure, and request terminalization | `remove-direct-database-access` | The scheduled job carries only the gate kind and opaque request ID and dispatches to the matching transactional request action. That action locks the exact gate and execution, snoozes before expiry, and otherwise atomically expires or supersedes the request and terminalizes only the matching captured waiting version with its durable event. | Approval and context-expansion expiry, resolved-gate no-op, stale-wait supersession, durable event, rollback, and architecture tests. | Give the expiry actions and nested execution and delivery operations policy-aware action contexts without broad bypasses. |
| `lib/office_graph/agent_runtime/services/output_router.ex` | `OfficeGraph.AgentRuntime` | `validate_definition_output_kind/2`, `validate_snapshot_capability/4` | Immutable execution-definition and invocation-authority output-governance lookups | `implement-internal-agent-runtime/agent-tool-adapters` | Before any owning-domain command runs, routing loads only the definition captured by the execution and the exact authority snapshot referenced by its immutable context package. It verifies the classified output is in the definition allowlist, the snapshot belongs to that execution, and any owning-domain capability required by the output remains in the captured grant set; a missing or mismatched record, storage failure, disallowed kind, or missing capability fails closed without a domain mutation. | Governed output allowlist and capability enforcement, no-hidden-side-effect, routed output, snapshot-lineage, and architecture tests. | Replace with policy-aware execution-definition and context-snapshot relationship reads or an output-routing action that preserves pre-mutation fail-closed enforcement. |
| `lib/office_graph/node_conversations.ex` | `OfficeGraph.NodeConversations` | `append_agent_message/5`, `append_human_message/3`, `read_conversation/3`, `read_messages/1`, `start/3` | Authorized action dispatch and bounded conversation/message reads | `remove-direct-database-access` | Public human commands validate their session-bound operation, digest, capability, and tenant before dispatching an owning transactional action. Agent output arrives from the already-authorized and locked execution worker action. Projection reads authorize `skeleton.read` and validate the exact run/graph packet-source contract before reading the one conversation and its bounded messages. | Conversation command, output routing, replay concurrency, scope, GraphQL/JSON parity, and architecture tests. | Propagate policy-aware actors through the transactional actions and give conversation/message reads policy-aware scoped read actions. |
| `lib/office_graph/node_conversations/projections/conversation_projection.ex` | `OfficeGraph.NodeConversations` | `read_invocation_target/2`, `read_limit/2`, `read_visible_context_packages/3` | Relationship-derived, tenant-scoped context, execution, gate, and invocation projection reads | `remove-direct-database-access` | The projection boundary has already authorized `skeleton.read` and validated the exact run/graph packet-source contract. These reads follow declared Ash relationships, enforce organization/workspace/run/graph scope, prioritize active executions and pending gates inside a hard 100-row bound, and return only typed Ash resources or redacted context summaries. | Context visibility/redaction, active-execution retention, pending-gate retention, invocation affordance, bounded history, API parity, and architecture tests. | Give the related AgentRuntime resources policies that accept the authenticated projection actor and remove the broad bypass. |
| `lib/office_graph/node_conversations/commands/conversation_commands.ex` | `OfficeGraph.NodeConversations` | `create_conversation/1`, `locked_conversation/1`, `lock_valid_run_scope/1`, `validate_graph_item_membership/2` | Action-owned run lock, packet-membership validation, conversation replay, and create | `remove-direct-database-access` | The owning transactional action has revalidated the authorized operation and exact tenant scope before these internal calls. The stable run lock serializes first conversation creation, and the declared conversation identity remains the database backstop. | Conversation command, first-start concurrency, packet scope, replay, and architecture tests. | Give the conversation and packet-source resources policies that accept the authenticated command actor through the owning action. |
| `lib/office_graph/node_conversations/commands/message_commands.ex` | `OfficeGraph.NodeConversations` | `agent_message/2`, `create_message/1`, `locked_active_conversation/2`, `message_for_operation/1` | Action-owned conversation lock, exact message replay, and create | `remove-direct-database-access` | Human actions revalidate the authorized operation and lock the scoped active conversation before replay or creation. Agent actions run inside the execution worker's owning transaction with its execution aggregate already locked and validate immutable execution/context provenance before replay or creation. | Human and agent message replay, separate-owner concurrency, output-router rollback, execution-worker, and architecture tests. | Give message command actions and their nested reads and creates policy-aware human and system actors. |
| `lib/office_graph/proposed_changes.ex` | `OfficeGraph.ProposedChanges` | `create_from_agent/6` | System-operation-scoped agent proposal replay read | `implement-internal-agent-runtime/agent-tool-adapters` | The owner revalidates the exact agent execution, step, context package, tenant, principal, and system operation before returning or creating only a pending proposal; it never applies graph truth. | Governed proposal and finding routing, replay, no-direct-mutation, and architecture tests. | Replace with a typed policy-aware proposal command for authenticated agent principals. |
| `lib/office_graph/runs.ex` | `OfficeGraph.Runs` | `record_agent_observation/5` | System-operation-scoped agent observation identity upsert and replay comparison | `implement-internal-agent-runtime/work-runs` | The owner revalidates the exact agent execution, step, context package, run, graph item, tenant, principal, and system operation before upserting and returning one immutable non-authoritative observation without reducing parent run state. | Governed observation routing, replay, unchanged-run-state, and architecture tests. | Replace with a policy-aware non-authoritative agent observation command. |
| `lib/office_graph/verification.ex` | `OfficeGraph.Verification` | `create_agent_evidence_candidate/5`, `first_required_check/2` | System-operation-scoped evidence-candidate replay and graph-item-qualified required-check selection reads | `implement-internal-agent-runtime/verification-evidence` | The owner revalidates the exact execution, context package, step, run, graph item, tenant, principal, and system operation, selects one required check for that graph item in the run contract, and returns or creates only an agent-reported candidate that cannot satisfy accepted-evidence policy by itself. | Governed evidence graph-item routing, replay, candidate-only behavior, no verification-result mutation, and architecture tests. | Replace with a policy-aware agent evidence-candidate command and relationship-backed required-check selection. |
| `lib/office_graph/content.ex` | `OfficeGraph.Content` | `persist_plain_document/3`, `plain_text_for_scope/2` | Internal document command dispatch and scope-checked document text reads | `remove-direct-database-access` | The public human or system content command validates the exact operation and capability before dispatching one transactional content action; reads receive an already-validated exact organization/workspace scope. | Content rollback and trace tests, WorkGraph persistence, integration signal refresh, and architecture conformance. | Give the transactional content action a policy-aware actor contract and express the remaining reads through policy-aware Content queries. |
| `lib/office_graph/content/document.ex` | `OfficeGraph.Content` | `create/2` | Nested document, block, and revision creation | `remove-direct-database-access` | The owning document action has already validated its authorized operation context and must create the three dependent records atomically while consuming their nested notifications. | Content rollback, document trace, and architecture conformance tests. | Give nested content records policies that accept the owning action context without a broad bypass. |
| `lib/office_graph/external_refs.ex` | `OfficeGraph.ExternalRefs` | `reference_by_lookup/2` | Provider-reference identity lookup after system-operation validation | `add-github-review-integration/github-review-integration` | The owning boundary resolves one organization, optional workspace, source, and external identity after authenticated reconciliation and rejects any provider or resource mismatch before updating safe projection fields. The lookup is also the read half of the boundary's conflict-safe get-or-insert contract for concurrent reconciliation. | Reconciliation ordering, replay, product mapping, concurrent identity creation, provider ownership, organization/workspace isolation, and architecture tests. | Replace with a policy-aware ExternalRefs upsert action that preserves tenant-scoped provider identity collision handling. |
| `lib/office_graph/identity.ex` | `OfficeGraph.Identity` | `active_principal?/1`, `active_system_principal/1`, `consume_oidc_login_transaction/1`, `ensure_owner/1`, `ensure_session_context/3`, `ensure_system_principal/2`, `store_oidc_login_transaction/1`, `store_oidc_login_transaction/2`, `validate_session_context/1` | Local bootstrap, pre-session OIDC guards, and human-session or system-principal validation | `remove-direct-database-access` | Local bootstrap runs before the resulting principal and session can authorize; OIDC initiation and callback consumption necessarily run before a human actor exists; authenticated service and webhook operation creation must validate or establish the exact system principal or session state inside the Identity boundary. | Foundation bootstrap, OIDC one-time consume, session, system-operation, failure-atomicity, and architecture conformance tests. | Replace local fixture bootstrap with onboarding and give pre-session OIDC and validation actions purpose-built policies that do not require a broad bypass. |
| `lib/office_graph/identity.ex` | `OfficeGraph.Identity` | `active_principal/1` | Agent-principal lifecycle validation before authority capture and reuse | `implement-internal-agent-runtime/agent-invocation` | Agent invocation resolves only the supplied principal ID and then requires active backend-principal posture before authority is captured or reused. | Agent principal revocation, tenant-isolation, and architecture tests. | Replace with a scoped principal read policy for authenticated agent-runtime commands. |
| `lib/office_graph/identity/resources/external_identity_link.ex` | `OfficeGraph.Identity` | `create_link/1`, `external_identity_link/2`, `external_links_for_email/1`, `locked_principal/1`, `principals_for_email/1`, `update/3` | Pre-session reconciliation reads, locks, creates, and updates | `remove-direct-database-access` | The transactional reconciliation action runs before a human session exists, constrains every lookup to an exact provider subject or normalized verified identifier, locks the selected principal, and emits only typed linked or review-required outcomes. | Exact-subject replay, cross-provider conflict, ambiguity, disabled identity, concurrency, and failure-atomicity tests. | Give the reconciliation action a purpose-built pre-session actor and policies for its exact reads and writes. |
| `lib/office_graph/identity/resources/oidc_login_transaction.ex` | `OfficeGraph.Identity` | `destroy_expired/1`, `run/3` | Pre-session OIDC guard creation, pruning, and one-time consumption | `remove-direct-database-access` | OIDC state is stored and consumed before an authenticated actor exists; the owning actions operate only on opaque hashed identifiers and bounded expiry state. | OIDC one-time consumption, replay rejection, pruning, and architecture conformance tests. | Give OIDC guard actions an explicit unauthenticated login-flow policy context. |
| `lib/office_graph/identity/resources/principal.ex` | `OfficeGraph.Identity` | `ensure/2` | Nested bootstrap principal creation | `remove-direct-database-access` | The owning local bootstrap action creates or replays the exact configured principal before that principal can authorize its own setup. | Owner bootstrap replay, rollback, uniqueness, and architecture conformance tests. | Replace local bootstrap with policy-aware onboarding. |
| `lib/office_graph/identity/resources/session.ex` | `OfficeGraph.Identity` | `create/2`, `create_event/1`, `locked_external_identity/1`, `locked_principal/1`, `locked_session/1`, `revoke_active/5`, `update/3` | Pre-actor session issue and revoke lifecycle internals | `remove-direct-database-access` | A cookie or reconciled identity is not yet an authenticated actor. Transactional session actions lock and revalidate the exact principal, link, and session before replacing or revoking lifecycle state and recording authentication evidence. | Session issue, replacement, expiry, revocation, disabled identity, stale-cookie, logout, concurrency, and architecture tests. | Give session lifecycle actions explicit pre-authentication policy contexts and remove the broad nested bypasses. |
| `lib/office_graph/identity/services/external_identity_reconciliation.ex` | `OfficeGraph.Identity` | `run_reconciliation_action/1` | Pre-session reconciliation action dispatch | `remove-direct-database-access` | Reconciliation necessarily runs before a human session actor exists and delegates the exact evidence envelope to one transactional resource action. | External-identity replay, conflict, ambiguity, lifecycle, concurrency, and architecture tests. | Introduce a purpose-built pre-session reconciliation actor accepted by the action policy. |
| `lib/office_graph/identity/services/human_sessions.ex` | `OfficeGraph.Identity` | `issue/4`, `record_event/1`, `resolve/2`, `revoke/2` | Pre-actor evidence, durable session validation, issue, and logout dispatch | `remove-direct-database-access` | Authentication evidence, opaque-cookie resolution, session issue, and logout all begin before the resulting human actor can be trusted; the boundary constrains reads and action input to the exact credential or reconciled identity. | Callback rejection, issue, replacement, expiry, revocation, disabled identity, stale-cookie, logout, web transport, and architecture tests. | Give authentication evidence and session lifecycle actions explicit pre-authentication policy contexts. |
| `lib/office_graph/tenancy.ex` | `OfficeGraph.Tenancy` | `run_ensure_local_scope/1`, `validate_workspace_scope/2` | Local bootstrap action dispatch and pre-session organization-workspace validation | `remove-direct-database-access` | Local setup creates its first tenant scope before an actor exists, while session issue and load must validate the stored tenant relationship before trusting that actor. | Local-scope rollback and replay, human-session scope, migration constraint, and architecture tests. | Replace local fixture bootstrap with onboarding and expose a policy-aware pre-session tenancy validation action. |
| `lib/office_graph/tenancy/organization.ex` | `OfficeGraph.Tenancy` | `ensure/2` | Nested local tenant bootstrap creation | `remove-direct-database-access` | The owning setup action creates exact organization, workspace, initiative, and workstream identities before a tenant actor exists. | Local-scope replay, rollback, uniqueness, and architecture conformance tests. | Replace local fixture bootstrap with policy-aware onboarding. |
| `lib/office_graph/integrations.ex` | `OfficeGraph.Integrations` | `archive_provider_delivery/1`, `ensure_source/3`, `read_provider_delivery_archive/2`, `submit_or_replay_manual_intake/3` | Provider archive, source identity, manual-intake action dispatch, and exact replay reads | `remove-direct-database-access` | Verified provider workers and already-authorized manual commands delegate to transactional owner actions; provider-source creation uses an exact identity upsert, and archive reads require the full source and delivery identity. | Manual intake replay and failure atomicity, provider delivery duplicate/conflict, webhook worker, and architecture conformance tests. | Give provider system principals and manual command actors policies accepted directly by the owning actions and reads. |
| `lib/office_graph/integrations/actions/submit_manual_intake.ex` | `OfficeGraph.Integrations` | `accepted_duplicate/2`, `create/2`, `duplicate_content_matches?/2`, `ensure_source/1`, `existing_command_intake/2`, `load_intake/2` | Transactional intake identity, replay, archive, event, and proposal internals | `remove-direct-database-access` | The owning action has validated an exact manual-intake operation and scope, then locks the operation and constrains every nested read or write to its source, replay, archive, and normalized-event identities. | Manual intake command replay, source convergence, content conflict, failure atomicity, and concurrency tests. | Propagate the authenticated manual-intake actor through policies on every nested resource. |
| `lib/office_graph/integrations/raw_archive.ex` | `OfficeGraph.Integrations` | `create_archive/1`, `existing_archive/2`, `locked_source/1` | Transactional provider-delivery archive replay internals | `remove-direct-database-access` | A verified provider system operation delegates to an action that locks the exact provider source and reads or creates only its immutable delivery archive identity. | Provider delivery replay, immutable-content conflict, webhook worker, and architecture conformance tests. | Propagate the authenticated provider actor through source and archive policies. |
| `lib/office_graph/operations.ex` | `OfficeGraph.Operations` | `create_operation/5`, `create_system_operation/1`, `existing_operation/3`, `existing_system_operation/1`, `lock_operation/1`, `lock_scoped_target/3`, `read_operation/1` | Human and system operation creation, idempotency replay lookup, operation locking and recovery reads, and shared scoped command-target locking | `add-github-review-integration/shared-operation-contracts` | Operation correlation is infrastructure created only after human session or system-principal authorization. Fresh packet-version commands and run starts share one organization/workspace-scoped target lock after public command authorization, system operations use an exact organization, optional workspace, principal, action, and idempotency scope, and worker recovery performs a non-locking read before the reconciliation boundary acquires its transaction-scoped terminalization lock. | Concurrency, cross-workspace and organization-scoped system replay, human operation replay, packet run, stale-version, durable delivery, webhook recovery, and evidence acceptance tests. | Introduce policy-aware operation and scoped target-lock or recovery-read commands, or Ash-managed atomic workflows. |
| `lib/office_graph/github_integration/commands/installation_commands.ex` | `OfficeGraph.GitHubIntegration` | `bind/3`, `create_credential_binding/5`, `create_installation/5`, `create_permission_entries/2`, `create_permission_snapshot/2`, `installation_by_operation/1`, `read_credentials/1`, `replay_binding/4`, `set_permission_snapshot/2` | Action-owned installation binding dispatch, identity upserts, nested aggregate writes, exact replay reads, and storage-failure classification | `remove-direct-database-access` | The public owner command validates the exact human operation, tenant scope, capability, and immutable binding envelope before dispatching one transactional installation action. The action locks the operation, upserts only the declared provider-installation and scoped credential identities, creates the permission snapshot aggregate and current pointer atomically, and reconstructs only the caller-owned replay result. Exact identity constraints serialize concurrent claims without provider-identity enumeration, and the shared installation lookup preserves classified storage failures. | Installation binding authorization, immutable replay, credential reuse, rollback, provider-identity collision, storage-failure classification, tenant non-disclosure, API, and architecture conformance tests. | Give the installation action and nested principal, role, permission, credential, and binding resources policy-aware action contexts without broad bypasses. |
| `lib/office_graph/github_integration/adapters/secret_store/secret_store.ex` | `OfficeGraph.GitHubIntegration` | `load_credential/1` | Credential metadata lookup after explicit tenant scope input | `add-github-review-integration/github-review-integration` | Secret resolution is intentionally outside product resource policies; the narrow boundary loads one credential by opaque ID, verifies exact organization/workspace scope and active state, then delegates the reference to the configured secret adapter. | Secret store tenant-isolation, missing-reference, environment-adapter, and architecture conformance tests. | Replace with managed secret infrastructure exposing a policy-aware scoped credential resolution command. |
| `lib/office_graph/github_integration/services/reconciler.ex` | `OfficeGraph.GitHubIntegration` | `authenticated_own_reply?/2`, `authorized_installation/2`, `base_by_extension/5`, `check_run_by_extension/3`, `check_run_extension!/3`, `ensure_check_run_extension!/3`, `ensure_extension!/5`, `extension_by_node!/3`, `locked_reconciliation_installation/2`, `missing_provider_references!/4`, `missing_provider_resources!/5`, `outcome_by_operation/2`, `persist_installation_failure!/4`, `persist_outcome!/2`, `persist_outcome_command/2`, `reconcile_snapshot/5`, `resolve_credential/2`, `terminalize_retry_outcome!/2` | Action-owned reconciliation dispatch, locked installation/outcome lifecycle, provider-extension writes and reads, exact outbound-reply authentication, and authoritative-absence reads | `remove-direct-database-access` | Reconciliation validates the system operation and credential before provider access, then dispatches provider persistence or classified outcome persistence to transactional SyncOutcome actions. Those actions lock the operation and installation through Ash, which serialize one operation outcome and all provider-object identities for an installation without advisory SQL; every provider-neutral resource, extension, reference, product signal, trace, lifecycle transition, and outcome remains inside the owning action. Exact tenant, source, pull-request, provider-response, and operation identities fail closed. | Reconciliation ordering, replay, forged-marker rejection, failure and terminalization classification, multi-PR projection, product mapping, authoritative absence, tombstoning and reappearance, revocation, failure rollback, concurrency, tenant-isolation, worker, and architecture tests. | Give the reconciliation system principal policies accepted directly by SyncOutcome and all nested provider, graph, trace, and lifecycle resources. |
| `lib/office_graph/github_integration/commands/outbound_commands.ex` | `OfficeGraph.GitHubIntegration` | `action_by_operation/1`, `active_installation/2`, `check_run_extension/1`, `create_action/5`, `persist_and_enqueue/4`, `require_installation_provenance/2`, `require_permission/2`, `review_comment_extension/1`, `scoped_target/3` | Action-owned outbound dispatch, locked authorization/target/provenance reads, nested action creation, and exact replay | `remove-direct-database-access` | The public command validates its operation envelope, then dispatches one transactional outbound action. That action locks the operation, installation, permission, target, provider extension, and reconciliation provenance before rechecking authorization and provider version; it creates the action, unique Oban job, audit, and revision as one outcome or returns the exact existing operation replay. | Outbound allowlist, replay, stale-version, installation provenance, permission, storage rollback, API, worker, and architecture tests. | Give the outbound action and its nested target, provenance, audit, and revision resources policy-aware action contexts without broad bypasses. |
| `lib/office_graph/github_integration/workers/outbound_worker.ex` | `OfficeGraph.GitHubIntegration` | `action/3`, `active_installation/1`, `credential_binding/1`, `current_target/2`, `locked_action/1`, `persist_revoked_outcome/2`, `revoke_installation_if_winner/1`, `trace/2`, `update_action/2`, `update_installation_lifecycle/2` | Durable outbound execution reads/writes plus action-owned revocation and trace-recovery dispatch | `remove-direct-database-access` | The worker accepts only the recorded action and exact tenant scope, rechecks installation, credential, target version, and review actionability immediately before provider access, and stages provider intent before persistence. Provider revocation dispatches one transactional OutboundAction action that locks the operation, action, and installation before atomically terminalizing the winning action and revoking installation access. Completed outcomes dispatch a separate transactional trace action that locks the same operation/action and idempotently restores audit and revision evidence without repeating provider mutation. | Outbound worker success, successful-write recovery, reconciled-success recovery, actionability and delayed stale-version rejection, retry exhaustion, terminal-write recovery, atomic revocation rollback/replay, completed-trace recovery, credential lifecycle, and architecture tests. | Give the outbound system principal policies accepted directly by action, installation, credential, target, audit, and revision actions. |
| `lib/office_graph/github_integration/services/health.ex` | `OfficeGraph.GitHubIntegration` | `scoped_installation/2`, `credentials/1`, `credential_records/1`, `failure_counts/2`, `last_success/1`, `permissions/1`, `recent_failure_actions/2`, `recent_failure_outcomes/2` | Bounded tenant-scoped integration health assembly | `add-github-review-integration/integration-health` | After projection authorization, health rechecks exact installation organization/workspace scope and performs a constant number of bounded reads over safe permission and credential status, filtered failure samples, complete aggregate failure counts, outbound failures, and the latest successful transition. It never reads secret references, action input, raw payloads, or job errors. | Integration health query-count, complete-count, transition-time, dependency-storage-failure, failure-limit, non-disclosure, cross-tenant, GraphQL/JSON, and architecture tests. | Replace with generated policy-aware aggregate projections when they preserve the same fixed query bound, complete headline counts, and safe field contract. |
| `lib/office_graph/github_integration/workers/webhook_worker.ex` | `OfficeGraph.GitHubIntegration` | `load_installation/3`, `private_key_credential/1`, `receipt_operation/2` | Durable reconciliation installation, credential-binding, and exact receipt-operation reads | `add-github-review-integration/github-review-integration` | The worker accepts only the verified job envelope, rechecks exact installation organization/workspace lifecycle, resolves only its app-private-key binding, delegates scoped archive retrieval and secret resolution to their owning boundaries before provider access, and reloads only the exact accepted receipt operation before durable pre-operation terminalization. | Webhook worker reconciliation, credential failure, replay, exhausted pre-operation terminalization, and architecture tests. | Replace with policy-aware system-principal installation, credential, and receipt-operation read actions. |
| `lib/office_graph/github_integration/services/webhook_receipt.ex` | `OfficeGraph.GitHubIntegration` | `active_installation/1`, `webhook_credential/1` | Pre-archive active installation and credential-binding lookup | `add-github-review-integration/github-review-integration` | The webhook boundary must select one installation and its webhook credential before signature verification; it returns only safe error atoms and performs no archive, operation, event, or job write until both checks and constant-time verification pass. | Webhook invalid-signature, unknown-installation, unsupported-event, duplicate, controller, and architecture tests. | Replace with policy-aware authenticated installation lookup once provider request principals can be represented before signature verification. |
| `lib/office_graph/github_integration/commands/webhook_receipt_commands.ex` | `OfficeGraph.GitHubIntegration` | `active_credential/2`, `locked_active_credential/3`, `locked_active_installation/1`, `record/6` | Action-owned verified-receipt dispatch and locked installation/credential revalidation | `remove-direct-database-access` | Only after installation lookup, scoped secret resolution, constant-time signature verification, event allowlisting, and payload validation does the receipt boundary dispatch one transactional installation action. The action re-locks the exact active installation, webhook binding, and credential scope before atomically creating or replaying the system operation, immutable archive, domain event, dispatch job, and reconciliation jobs. | Webhook signature, unknown-installation, duplicate, payload, credential, storage-failure rollback, controller, worker, and architecture conformance tests. | Represent verified provider requests with a policy-aware provider actor accepted by the receipt action and its nested resources. |
| `lib/office_graph/projections/operator_workflow.ex` | `OfficeGraph.Projections` | `graph_relationships_for_links/2`, `read_audit_records_by_operation_id/1`, `read_intake_event/2`, `read_intake_events/3`, `read_packet_versions_with_runs/2`, `read_proposed_changes_by_event_id/2`, `read_revision_records_by_operation_id/1`, `read_scoped_many/3`, `read_work_packet_required_checks/2`, `read_work_packet_source_references/2` | Read-only operator workflow projection assembly | `remove-direct-database-access` | After `skeleton.read` authorization, the projection batches tenant-scoped Ash resources, follows packet-version required-check and run relationships, derives exact mixed-resource counts from typed collections, and applies the stable detail cursor to the bounded public projection. | Operator workflow detail/count parity, exact packet predicate, multi-version run recency, pagination, query-count, and architecture tests. | Replace the remaining broad internal reads with policy-aware projection actions that accept the authenticated session actor. |
| `lib/office_graph/projections/agent_context.ex` | `OfficeGraph.Projections` | `external_entries/2` | Read-only external-reference context assembly for an already-authorized graph item | `implement-internal-agent-runtime/agent-invocation` | The projection first authorizes the backend agent and obtains the selected item through WorkGraph's scoped context boundary. It then reads only external references matching the exact organization, workspace posture, resource type, and resource identity and emits opaque reference records without provider payloads or secrets. | Agent context external-reference, cross-tenant redaction, forbidden-context, and architecture tests. | Replace with a policy-aware ExternalRefs projection action accepting the already-authorized selected-item contract. |
| `lib/office_graph/projections/packet_readiness.ex` | `OfficeGraph.Projections` | `read_graph_items/2`, `read_verification_checks/2` | Read-only packet readiness projection assembly | `stabilize-architecture-foundation/ui-projection-contracts` | Packet readiness reads validate source graph items and required checks before packet creation while resource projection contracts are still being stabilized. | Operator workflow projection and API parity tests. | Replace with policy-aware projection contracts and generated reads where safe. |
| `lib/office_graph/projections/packet_workspace.ex` | `OfficeGraph.Projections` | `read_current_version/2`, `read_packet/2`, `read_required_checks/2`, `read_source_references/2`, `read_verification_checks/3` | Read-only packet readiness and affordance projection assembly | `complete-operator-command-loop/packet-workspace` | The narrow mixed projection reads one already-authorized, organization/workspace-scoped current packet contract to derive readiness, blockers, and command affordances. Generated AshGraphql and AshJsonApi packet, version, source-reference, and required-check reads own all resource-shaped API fields and version pagination. | Packet workspace projection authorization tests, generated resource/node tests, and GraphQL API parity tests. | Replace the remaining derived projection reads with a policy-aware Ash calculation or typed read action. |
| `lib/office_graph/projections/run_state.ex` | `OfficeGraph.Projections` | `read_evidence_candidates/3` | Read-only run state projection assembly | `stabilize-architecture-foundation/ui-projection-contracts` | Run state reads evidence candidates alongside run summaries while resource projection contracts are still being stabilized. | Operator workflow projection and API parity tests. | Replace with policy-aware projection contracts and generated reads where safe. |
| `lib/office_graph/proposed_changes.ex` | `OfficeGraph.ProposedChanges` | `apply_all/3`, `ash_update_internal!/3`, `create_for_manual_intake/4`, `lock_normalized_event/1`, `read_existing_for_normalized_event/2`, `validate_accepted_normalized_event/2`, `validate_reference_scope/2` | Transactional proposal intake, application, locking, and scoped reference validation internals | `remove-direct-database-access` | Authorized command boundaries dispatch to owning transactional actions. Manual intake locks the exact normalized event; apply locks the exact proposal set; conversation linkage validates one supplied proposal ID against the already-authorized session tenant before its message action writes. | Proposal validation precedence, replay, authorization evidence, graph lineage, conversation reference scope, integration failure atomicity, and architecture conformance tests. | Give proposal command actions and their nested graph writes and reference validation policies that accept the authenticated command actor directly. |
| `lib/office_graph/proposed_changes/proposed_graph_change/originating_operation_actor.ex` | `OfficeGraph.ProposedChanges` | `fetch_operation/1` | Resource policy operation lookup | `stabilize-architecture-foundation/backend-model-ownership` | The private policy check validates proposal operation ownership without exposing the operation read publicly. | ProposedChanges resource and API tests. | Replace with typed proposal validation actions or delete with old proposed graph changes. |
| `lib/office_graph/proposed_changes/proposed_graph_change/trace_reference_scope.ex` | `OfficeGraph.ProposedChanges` | `fetch_reference/2` | Resource validation reads | `stabilize-architecture-foundation/backend-model-ownership` | The private change validates operation and normalized-event trace references without exposing those reads publicly. | ProposedChanges resource and API tests. | Replace with typed proposal validation actions or delete with old proposed graph changes. |
| `lib/office_graph/proposed_changes/proposed_graph_change/validate_pending_update.ex` | `OfficeGraph.ProposedChanges` | `validate_current_status/2` | Internal status validation | `stabilize-architecture-foundation/backend-model-ownership` | The private change locks and validates the current proposal state before mutation. | ProposedChanges resource and API tests. | Replace with typed proposal validation actions or delete with old proposed graph changes. |
| `lib/office_graph/proposed_changes/proposed_graph_change/validate_unique_normalized_event_change_type.ex` | `OfficeGraph.ProposedChanges` | `validate_unique_event_change_type/3` | Internal uniqueness validation | `stabilize-architecture-foundation/backend-model-ownership` | The private change checks proposal uniqueness before create while the database identity remains authoritative under races. | ProposedChanges resource and API tests. | Replace with typed proposal validation actions or delete with old proposed graph changes. |
| `lib/office_graph/revisions.ex` | `OfficeGraph.Revisions` | `record!/5`, `record_once!/5`, `count_for_operation/1` | Revision trace writes, idempotent trace-recovery reads, and operation-scoped counts | `stabilize-architecture-foundation/backend-model-ownership` | Revisions are internal trace records created after public commands have authorized the product operation. Idempotent recovery reads use the exact operation, resource type, resource ID, and revision type while the caller holds the operation lock. | Architecture conformance plus workflow tests that assert revision traces and completed outbound replay without provider re-execution. | Replace with owner-scoped revision actions or policy-aware revision write and ensure commands. |
| `lib/office_graph/runs.ex` | `OfficeGraph.Runs` | `active_run_for_packet_version/2`, `create_observation_record/4`, `create_run_contract/5`, `create_run_required_checks/3`, `existing_observation/2`, `existing_observation_for_operation/2`, `existing_run_result/4`, `failed_observations_for_run?/1`, `fetch_scoped/3`, `lock_required_checks_for_run/1`, `lock_run/1`, `lock_scoped_run/2`, `mark_required_check_in_locked_run/3`, `observation_operation_idempotency_key_matches?/3`, `packet_required_checks/1`, `projection_child_counts/1`, `read_run_children/2`, `read_run_required_checks/2`, `record_observation/4`, `required_checks_for_run/1`, `run_verification_action/2`, `set_run_verification_failed/1`, `set_run_verified/1`, `start_run/4`, `update_run_after_observation/2`, `update_run_failed/1`, `validate_required_check_contract/2` | Transactional run command dispatch, nested writes, aggregate loads, replay reads, scoped locks, packet-contract validation, and verification lifecycle updates | `remove-direct-database-access` | Public commands authorize before dispatching owning transactional Run actions. Those actions lock the operation and owning packet or run through Ash, constrain nested reads and writes to the exact tenant and declared identities, use identity-backed observation replay, and recompute lifecycle state from locked children. Projection counts load scoped Ash aggregates after the projection boundary has authorized the run. | Work-run start, observation replay, verification and waiver lifecycle, rollback, projection query-count, GraphQL command, and architecture conformance tests. | Propagate the authenticated Runs command actor through policies on every nested resource and replace the remaining internal bypasses with policy-aware relationships and actions. |
| `lib/office_graph/runs.ex` | `OfficeGraph.Runs` | `fetch_agent_record/3`, `fetch_agent_run/3`, `validate_run_graph_item/2` | Agent-context run, packet, version, child-record, and selected-item validation reads | `implement-internal-agent-runtime/agent-invocation` | The agent-context handoff first authorizes the backend principal, then constrains the run, packet, version, child records, and selected graph item to one exact organization/workspace and packet-source contract. | Agent-context scope, required-check, forbidden-item, tenant-isolation, and architecture tests. | Replace with a policy-aware Runs agent-context action that preserves the exact packet-source contract. |
| `lib/office_graph/runs/changes/scoped_read.ex` | `OfficeGraph.Runs` | `fetch_with_lock/7` | Shared private scoped validation read | `stabilize-architecture-foundation/backend-model-ownership` | Run change modules share scoped read helpers to validate supplied run and packet-version references without exposing those reads publicly; run start locks the owning packet before proving that the requested version is still current. | Runs action-contract conformance and packet-run tests. | Replace with Ash relationship-backed validation or fully Ash-managed run-start and observation workflows. |
| `lib/office_graph/runs/changes/validate_run_start_contract.ex` | `OfficeGraph.Runs` | `packet_required_checks/1`, `packet_source_graph_item_ids/1`, `read_verification_checks/2` | Private run-start validation reads | `stabilize-architecture-foundation/backend-model-ownership` | Run create validation checks selected packet version scope, packet ownership, readiness child links, source/check pairing, and authority posture inside the private resource action before a run can start. | Runs action-contract conformance and packet-run tests. | Replace with Ash relationship-backed validation or fully Ash-managed run-start workflow. |
| `lib/office_graph/runs/changes/validate_observation_run_references.ex` | `OfficeGraph.Runs` | `fetch_verification_check/1`, `packet_source_graph_item?/2`, `required_check_graph_item?/2`, `run_requires_check?/2` | Private change validation reads | `stabilize-architecture-foundation/backend-model-ownership` | Observation create validation checks run, packet, source, and verification references inside private resource changes. | Runs resource validation tests and packet-run tests. | Replace with Ash relationships or domain validation contracts. |
| `lib/office_graph/runs/changes/validate_run_required_check_contract.ex` | `OfficeGraph.Runs` | `read_runs/1`, `read_packet_required_checks/2` | Batched private required-check contract validation reads | `eliminate-backend-query-fanout/backend-query-efficiency` | Required-check creation validates all scoped run and packet-version contracts with at most one Ash read per source inside the private batch resource change. | Runs action-contract query-count regressions and packet-run tests. | Replace with Ash relationship-backed validation or a policy-aware domain validation contract. |
| `lib/office_graph/repo.ex` | `OfficeGraph.Repo` | `ash_bulk_create!/2`, `ash_create!/2`, `ash_update!/1`, `fetch_by_lookup/2` | Shared internal Ash writes, ordered bulk child creation, and bootstrap lookup | `eliminate-backend-query-fanout/backend-query-efficiency` | Domain commands call these helpers after their public authorization or local bootstrap checks so shared internal Ash writes, ordered bulk child creation, and bootstrap refetches stay centralized and ledgered. | Architecture conformance plus the domain tests that exercise Runs, Verification, WorkPackets, Identity, Tenancy, and Authorization flows, including bulk rollback and result-order regressions. | Replace with policy-aware command actions or Ash-managed onboarding and child-creation workflows that no longer need broad internal bypass helpers. |
| `lib/office_graph/verification.ex` | `OfficeGraph.Verification` | `accept_evidence_candidate/4`, `create_evidence_candidate/3`, `create_evidence_candidate_record/3`, `existing_acceptance_for_operation/2`, `existing_candidate_for_operation/2`, `lock_candidate/1`, `persist_candidate_acceptance/4`, `preflight_result_slot/2`, `read_verification_result_for_evidence/1` | Authorized evidence action dispatch plus action-owned candidate creation, acceptance, replay, locking, result-slot preflight, and nested evidence writes | `remove-direct-database-access` | Public evidence commands authorize the exact operation before dispatching transactional Ash actions. Those actions lock the operation and candidate aggregate, constrain nested reads and writes to the command tenant, preserve exact replay, and atomically update evidence, verification, required-check, run, audit, and revision state. | Verification, packet-run, rollback, concurrency, transport, and architecture conformance tests. | Propagate the authenticated Verification command actor through policies on every nested resource and replace the remaining internal bypasses with policy-aware relationships and actions. |
| `lib/office_graph/verification/command_support.ex` | `OfficeGraph.Verification` | `fetch_scoped/3`, `lock_scoped!/3` | Shared scoped reads and target locking for verification commands | `complete-operator-command-loop/verification-evidence` | Verification command support centralizes already-authorized scoped reads and target locks shared by evidence and waiver workflows. | Verification, waiver, packet-run, and concurrency tests. | Replace shared bypass helpers with policy-aware scoped Verification reads. |
| `lib/office_graph/verification/waiver.ex` | `OfficeGraph.Verification` | `execute/5`, `existing_waiver_for_operation/2`, `lock_run_required_checks/1`, `persist_waiver/6` | Authorized waiver action dispatch plus action-owned replay, required-check locking, result creation, and run mutation | `remove-direct-database-access` | The public waiver command authorizes the exact operation before dispatching one transactional Ash action. The action locks the operation and run aggregate, rechecks expected state and packet membership, preserves exact replay, and atomically updates the verification result, required check, run, audit, and revision state. | Waiver, packet-run, rollback, concurrency, transport, and architecture conformance tests. | Propagate the authenticated Verification command actor through policies on every nested resource and replace the remaining internal bypasses with policy-aware relationships and actions. |
| `lib/office_graph/work_graph/commands/command_support.ex` | `OfficeGraph.WorkGraph` | `ash_create_internal/3`, `ash_get/2`, `ash_get_for_update/2`, `ash_read_one_internal/1`, `ash_update_internal/3` | Graph resource creation, internal lifecycle updates, replay reads, and scoped lock reads | `implement-typed-graph-relationships/graph-relationships` | WorkGraph owns typed graph truth writes, lifecycle updates, replay reads, and locks inside explicit authorized relationship transactions while command modules are split. | WorkGraph relationship command, replay, lifecycle, persistence, authorization, and architecture tests. | Express graph creation, replay, and lifecycle transitions as fully Ash-managed WorkGraph commands. |
| `lib/office_graph/work_graph/commands/proposal_commands.ex` | `OfficeGraph.WorkGraph` | `run_contract/4` | Authorized action-owned node creation dispatch | `remove-direct-database-access` | Public node commands dispatch to owning transactional actions that lock and revalidate the exact operation, actor session, action, capability, and parent resource before persisting documents, graph items, typed nodes, relationships, audit, and revisions. | Proposed-change application, typed-flow authorization, WorkGraph persistence, rollback, and architecture conformance tests. | Propagate the authenticated actor through policies on every nested resource so the action can run with normal authorization. |
| `lib/office_graph/work_graph/commands/relationship_commands.ex` | `OfficeGraph.WorkGraph` | `restore/4`, `run_relationship_contract/5`, `run_simple_relationship_contract/4` | Authorized action-owned relationship command dispatch | `remove-direct-database-access` | Public relationship commands enter private transactional GraphRelationship actions that lock the operation and target relationship, validate endpoint and provenance scope, enforce command-specific authorization and lifecycle rules, and atomically persist edge, audit, and revision changes. | Relationship command, scope, lifecycle, replay, cycle-concurrency, rollback, and architecture conformance tests. | Give relationship actions policies that accept the authenticated command actor and expose typed public action contracts. |
| `lib/office_graph/work_graph/policies/relationship_cycle_policy.ex` | `OfficeGraph.WorkGraph` | `lock_definition!/1`, `outgoing_edges/4` | Transaction-scoped definition lock and bounded active-edge traversal | `remove-direct-database-access` | For cycle-forbidden definitions, the owning relationship action locks the definition row through Ash before reading tenant- and definition-scoped active edges in deterministic bounded breadth-first batches. This serializes distinct operations without advisory locks and returns typed cycle or traversal-limit errors. | Reciprocal-edge behavior, distinct-operation cycle concurrency, relationship lifecycle, and architecture conformance tests. | Replace the internal bypasses with policy-aware definition and active-edge reads when relationship command actions carry their authenticated actor through all resources. |
| `lib/office_graph/work_graph/commands/system_commands.ex` | `OfficeGraph.WorkGraph` | `active_reference_relationship/3`, `ensure_reference_item!/3`, `fetch_reference_item!/2`, `reference_item_for_update!/2`, `signal_by_graph_item/1` | System-operation-scoped product mapping replay and canonical-identity reads | `add-github-review-integration/github-review-integration` | After validating the integration service operation and exact organization/workspace scope, WorkGraph resolves and locks one persisted external-reference graph item plus the active typed edge asserted by that integration principal, so concurrent provider replay returns the canonical item and integration-owned signal without colliding with user-authored signals that reference the same provider object. The same scoped lock/read path lets reconciliation refresh, close, or reopen the canonical signal as provider content and actionability change without creating placeholder work for first-seen inactive items. | Product mapping replay, user-authored reference coexistence, signal content refresh, signal lifecycle, signal concurrency, reconciliation concurrency, typed relationship, and architecture tests. | Replace with a policy-aware atomic integration-signal command and relationship-backed lookup. |
| `lib/office_graph/work_graph/queries/queries.ex` | `OfficeGraph.WorkGraph` | `authorized_item/2`, `batch_authorized_endpoints/2`, `read_adjacency/3`, `read_relationship/2` | Authorization-filtered relationship projection reads | `implement-typed-graph-relationships/graph-relationships` | Relationship adjacency and Relay node refetch perform one trusted capability check, then use explicit organization/workspace filters to read the relationship anchor and all visible endpoints in bounded batches; omitted endpoints are redacted rather than leaked. | Relationship query-count, endpoint-redaction, GraphQL, JSON, Relay-node, and architecture tests. | Replace with a generated policy-aware adjacency read when it preserves bounded batching and endpoint-level redaction. |
| `lib/office_graph/work_graph/commands/verification_commands.ex` | `OfficeGraph.WorkGraph` | `complete_verification/4`, `lock_review_findings_for_task!/1`, `lock_verification_checks_for_findings!/1`, `satisfy_verification_check_from_evidence/3` | Authorized action-owned verification dispatch plus parent-child lock reads | `remove-direct-database-access` | Public verification commands enter private transactional VerificationCheck actions that lock and revalidate the operation, authorization, check, ordered parent graph, and result inputs before atomically writing evidence or satisfaction state. Parent-before-child lock ordering preserves concurrent completion correctness. | Verification authorization, parent lifecycle, evidence acceptance, persistence rollback, concurrency, and architecture conformance tests. | Give verification actions and nested resources policies that accept the authenticated command actor, retaining only the stable relationship lock order. |
| `lib/office_graph/work_graph/changes/validate_evidence_candidate_references.ex` | `OfficeGraph.WorkGraph` | `matching_observation?/5`, `scoped_verification_check/3`, `validate_operation_context/4`, `validate_run_requires_check/4` | Private evidence-candidate relationship validation reads | `remove-direct-database-access` | The owning transactional evidence-candidate action has already authorized the exact create operation. Its private change uses modeled resources and exact tenant, run, check, observation, operation, and actor-session filters to validate supplied relationship identities without exposing public cross-domain reads. Storage failures become field-specific validation errors. | Evidence-candidate scope, actor-operation, graph-only observation, replay, transport, and architecture conformance tests. | Propagate the authenticated Verification actor through policies on the related resources and replace these internal bypasses with policy-aware relationship validation. |
| `lib/office_graph/work_graph/changes/validate_relationship_endpoints.ex` | `OfficeGraph.WorkGraph` | `fetch_graph_items/1` | Private graph relationship endpoint validation reads | `stabilize-architecture-foundation/backend-model-ownership` | Relationship create validation checks graph item endpoints inside a private resource change. | WorkGraph validation and persistence tests. | Replace with Ash relationships or domain validation contracts. |
| `lib/office_graph/work_graph/changes/validate_same_scope_references.ex` | `OfficeGraph.WorkGraph` | `fetch_references/2` | Batched private same-scope reference validation reads | `eliminate-backend-query-fanout/backend-query-efficiency` | WorkGraph batch create actions validate all referenced records with one scoped Ash read per resource without exposing broad cross-domain reads. | Architecture same-scope query-count and validation-equivalence tests. | Replace with modeled Ash relationships and policy-aware domain read contracts. |
| `lib/office_graph/work_graph/resources/review_finding.ex` | `OfficeGraph.WorkGraph` | `validate_open_task/2` | Private parent-state validation read | `stabilize-architecture-foundation/backend-model-ownership` | Review finding creation checks parent task state inside the WorkGraph boundary. | WorkGraph persistence tests. | Replace with Ash relationship and invariant change owned by WorkGraph. |
| `lib/office_graph/work_graph/resources/verification_check.ex` | `OfficeGraph.WorkGraph` | `validate_open_review_finding/2` | Private parent-state validation read | `stabilize-architecture-foundation/backend-model-ownership` | Verification check creation checks parent review finding state inside the WorkGraph boundary. | WorkGraph persistence tests. | Replace with Ash relationship and invariant change owned by WorkGraph. |
| `lib/office_graph/work_graph/verification_result/validate_result_evidence.ex` | `OfficeGraph.WorkGraph` | `fetch_evidence_item/1` | Private evidence result validation read | `complete-operator-command-loop/verification-evidence` | Verification result creation requires matching accepted evidence for passed or failed results and forbids fabricated evidence for governed waiver results. | Verification, waiver, and WorkGraph tests. | Move result recording ownership fully to Verification commands and replace the internal read with relationship-backed validation. |
| `lib/office_graph/work_packets.ex` | `OfficeGraph.WorkPackets` | `create_next_version_records/4`, `create_packet/3`, `create_packet_children/2`, `create_packet_contract/3`, `create_version/4`, `existing_packet_result/2`, `existing_version_result/3`, `read_current_version/1`, `read_required_checks/1`, `read_required_verification_checks/2`, `read_source_references/1`, `read_version_for_operation/2` | Transactional packet command dispatch, nested writes, idempotency replay, current-version reads, and ordered packet-contract reads | `remove-direct-database-access` | Public commands authorize before dispatching owning transactional actions. Those actions lock the operation and packet aggregate, constrain every nested write and replay read to the exact tenant and operation identities, bulk-create ordered children, and preserve typed stale or replay outcomes. | Work-packet query bounds, validation, delayed replay, stale-version, rollback, GraphQL command, and architecture conformance tests. | Propagate the authenticated packet command actor through policies on every nested resource and remove the remaining internal bypasses. |
| `lib/office_graph/work_packets/changes/validate_current_version.ex` | `OfficeGraph.WorkPackets` | `fetch_current_version/1` | Private current-version validation read | `stabilize-architecture-foundation/backend-model-ownership` | WorkPacket current-version updates must prove the selected version belongs to the packet and target scope inside the private Ash update action before the packet pointer changes. | WorkPackets action-contract tests and architecture conformance. | Replace with an Ash relationship-backed validation or fully Ash-managed packet-version lifecycle command. |
