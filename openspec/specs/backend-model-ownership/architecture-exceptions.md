# Architecture Exception Ledger

This ledger records the remaining approved direct Ecto mutation paths after the
WorkGraph Ash repair. Normal walking-loop product resources should move through
Ash resources and domain actions. Entries here are explicit exceptions that keep
graph identity, relationship, bootstrap, intake, change-proposal, and
maintenance writes visible until a later change narrows or retires them.

| File | Owner | Approved functions | Allowed operation type | Approving spec | Reason | Retirement condition |
| --- | --- | --- | --- | --- | --- | --- |
| `lib/office_graph/work_graph.ex` | `OfficeGraph.WorkGraph` | `{graph_transaction/1, Repo.transaction}` | Transaction boundary only | `repair-ash-model-conformance/backend-model-ownership` | WorkGraph graph identity, relationship, typed resource, and document writes still share one explicit transaction boundary so partially-created graph state is not committed. Table-backed WorkGraph records now use Ash actions inside that boundary. | Retire or narrow this entry when WorkGraph creation is expressed as a fully Ash-managed atomic workflow. |
| `lib/office_graph/integrations.ex` | `OfficeGraph.Integrations` | `{insert_manual_intake/4, Repo.transaction}`, `{insert_source_then_refetch/1, Repo.insert_all}` | Transaction boundary plus idempotent conflict insert | `repair-ash-model-conformance/backend-model-ownership` | Manual intake creates or reads integration source, raw archive, and normalized replay/idempotency records through Ash actions inside one explicit transaction boundary before change proposals are applied. External source creation uses one `ON CONFLICT DO NOTHING` insert plus an Ash refetch so first concurrent intakes sharing a new source do not poison the transaction on a unique-key race. | Retire or narrow this entry when manual intake storage is expressed as a fully Ash-managed atomic workflow or Ash upsert handles this external-source race without direct SQL. |
| `lib/office_graph/identity.ex` | `OfficeGraph.Identity` | `{ensure_owner/1, Repo.transaction}`, `{ensure_session_context/3, Repo.transaction}`, `{insert_then_fetch!/3, Repo.insert_all}` | Local bootstrap transaction plus idempotent conflict insert | `repair-ash-model-conformance/backend-model-ownership` | The walking skeleton needs a controlled local owner/principal/session bootstrap path before the full identity provider work exists. Identity bootstrap uses one `ON CONFLICT DO NOTHING` insert per missing principal, profile, or session plus an Ash refetch so first concurrent owner bootstraps do not poison the transaction on a unique-key race. | Replace or narrow this when authentik/Keycloak/SCIM identity flows are implemented, or retire the direct insert if Ash upsert handles this bootstrap race without direct SQL. |
| `lib/office_graph/tenancy.ex` | `OfficeGraph.Tenancy` | `{ensure_local_scope/1, Repo.transaction}`, `{insert_then_fetch!/3, Repo.insert_all}` | Local bootstrap transaction plus idempotent conflict insert | `repair-ash-model-conformance/backend-model-ownership` | Development and test bootstrap needs first organization, workspace, initiative, and default workstream records before product flows can run. Tenancy bootstrap uses one `ON CONFLICT DO NOTHING` insert per missing row plus an Ash refetch so first concurrent bootstraps for the same scope do not poison the transaction on a unique-key race. | Replace with a setup/onboarding domain when tenant provisioning is designed, or retire the direct insert if Ash upsert handles this bootstrap race without direct SQL. |
| `lib/office_graph/authorization.ex` | `OfficeGraph.Authorization` | `{ensure_owner_role/2, Repo.transaction}`, `{insert_then_fetch!/3, Repo.insert_all}` | Local bootstrap transaction plus idempotent conflict insert | `repair-ash-model-conformance/backend-model-ownership` | The first owner role, capability, policy anchor, and role assignment are bootstrap prerequisites for the skeleton authorization path. Authorization bootstrap uses one `ON CONFLICT DO NOTHING` insert per missing capability, role, role-capability link, role assignment, or policy bundle plus an Ash refetch so first concurrent owner bootstraps do not poison the transaction on a unique-key race. | Replace with policy bundle and role-management actions when authorization administration is implemented, or retire the direct insert if Ash upsert handles this bootstrap race without direct SQL. |
| `lib/office_graph/proposed_changes.ex` | `OfficeGraph.ProposedChanges` | `{create_for_manual_intake/4, Repo.transaction}`, `{apply_all/3, Repo.transaction}` | Transaction boundary only | `repair-ash-model-conformance/backend-model-ownership` | Manual intake creates the four pending change proposals through Ash actions inside one explicit transaction boundary, and change-proposal application locks pending rows, creates graph truth, and marks changes applied inside one explicit transaction so retry races cannot commit partial graph state. | Retire or narrow this entry when change-proposal intake and application are expressed as fully Ash-managed atomic workflows. |
| `lib/office_graph/content.ex` | `OfficeGraph.Content` | `{create_plain_document/3, Repo.transaction}` | Transaction boundary only | `repair-ash-model-conformance/backend-model-ownership` | Plain document creation writes the document, first paragraph block, and initial revision through Ash actions inside one explicit transaction boundary. | Retire or narrow this entry when Content creation is expressed as a fully Ash-managed atomic workflow. |
| `lib/office_graph/packet_run_verification.ex` | `OfficeGraph.PacketRunVerification` | `{execute_transaction/2, Repo.transaction}` | Composite workflow transaction boundary only | `stabilize-architecture-foundation/ash-domain-boundaries` | The packet-run-verification command composes packet preparation, run start, observation recording, evidence suggestion, evidence acceptance, and summary reads through owning domain commands. The outer transaction prevents a later observation-source or evidence validation conflict from committing partial per-step operation, packet, or run state under a flow identity while the one-shot workflow remains live. | Retire or narrow this entry when packet preparation, run start, observation recording, evidence suggestion or acceptance, and verification recomputation can be composed through fully Ash-managed atomic workflow commands without an outer transaction. |
| `lib/office_graph/work_packets.ex` | `OfficeGraph.WorkPackets` | `{create_packet_records/3, Repo.transaction}` | Transaction boundary only | `implement-work-packet-run-verification-slice` | Packet creation writes the packet, first stable version, typed source references, required check references, and current-version pointer through Ash actions inside one explicit transaction boundary. | Retire or narrow this entry when packet creation is expressed as a fully Ash-managed atomic workflow. |
| `lib/office_graph/work_graph/changes/validate_evidence_candidate_references.ex` | `OfficeGraph.WorkGraph` | `{validate_operation_context/4, Repo.query!}`, `{row_exists?/2, Repo.query!}` | Validation read only | `ash-api-surface` | Evidence candidate direct-create validation needs cross-context read probes against operations, run required checks, and execution observations without adding a WorkGraph-to-Runs module dependency cycle; the queries only validate supplied references and do not mutate state. | Retire or narrow this entry when evidence candidate reference validation can use an Ash-managed cross-context validation contract without circular dependencies. |
| `lib/office_graph/runs.ex` | `OfficeGraph.Runs` | `{create_run_records/4, Repo.transaction}`, `{create_observation/4, Repo.transaction}`, `{mark_required_check_satisfied/2, Repo.transaction}`, `{satisfy_required_check_and_verify_run/2, Repo.transaction}`, `{set_run_verified_if_all_required_checks_satisfied/1, Repo.transaction}`, `{lock_observation_idempotency_key!/2, Repo.query!}` | Transaction boundary plus transaction-scoped advisory lock | `implement-work-packet-run-verification-slice` | Work-run start revalidates persisted packet readiness, copies scoped required checks from the selected packet version, observation recording writes the child observation plus aggregate run state, direct child-row Ash actions validate the target run contract, required-check satisfaction locks the parent run before recomputing verification state, and observation replay checks compare both source-idempotency and operation-id replay facts while a transaction-scoped advisory lock prevents concurrent same-key flows from committing partial packet/run state or surfacing database unique-key errors before one observation wins. | Retire or narrow this entry when work-run start, observation recording, run verification recomputation, and observation-source replay serialization are expressed as fully Ash-managed atomic workflows. |
| `lib/office_graph/verification.ex` | `OfficeGraph.Verification` | `{create_evidence_candidate_record/3, Repo.transaction}`, `{accept_evidence_candidate_record/4, Repo.transaction}` | Transaction boundary only | `implement-work-packet-run-verification-slice` | Evidence candidate creation and acceptance lock operation/candidate rows, validate direct candidate create contracts through private Ash actions, create accepted evidence, record verification results, update required-check state when a run is present, update run verification state, and compare candidate/acceptance replay facts inside explicit transaction boundaries so operation replays cannot duplicate durable evidence records or return stale records for changed input. | Retire or narrow this entry when evidence candidate creation and acceptance are expressed as fully Ash-managed atomic workflows. |

## Authorization Bypass Ledger

This ledger records broad `authorize?: false` Ash call sites. These entries are
not approval for new bypasses; they make existing bypass scope explicit so
architecture conformance can reject untracked growth.

| File | Owner | Approved functions | Bypass scope | Approving spec | Reason | Verification coverage | Retirement condition |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `lib/office_graph/audit.ex` | `OfficeGraph.Audit` | `record!/5`, `count_for_operation/1` | Trace write and operation-scoped trace count | `stabilize-architecture-foundation/backend-model-ownership` | Audit records are internal trace records created after public commands have already authorized the product operation. | Architecture conformance plus workflow tests that assert audit traces. | Replace with owner-scoped trace actions or policy-aware audit write commands. |
| `lib/office_graph/authorization.ex` | `OfficeGraph.Authorization` | `get_or_create!/3`, `granted_capability?/2`, `insert_then_fetch!/3`, `record_decision/5`, `role_assignment_exists?/2`, `role_ids_for_capability/2` | Bootstrap, policy lookup, decision trace, and capability checks | `stabilize-architecture-foundation/backend-model-ownership` | Bootstrap and capability evaluation run before full authorization administration exists and must inspect policy rows directly inside the authorization boundary. | Foundation authorization tests and architecture conformance. | Replace with policy bundle and role-management actions with explicit read policies. |
| `lib/office_graph/content.ex` | `OfficeGraph.Content` | `ash_create/2`, `plain_text_for_document/2` | Internal document creation and document text reads | `stabilize-architecture-foundation/backend-model-ownership` | Content commands create document, block, and revision records after caller authorization is checked by the owning workflow. | WorkGraph persistence and content trace tests. | Express document creation and reads through policy-aware Content commands. |
| `lib/office_graph/identity.ex` | `OfficeGraph.Identity` | `active_principal?/1`, `fetch_existing/2`, `validate_session_context/1` | Local owner bootstrap and session validation | `stabilize-architecture-foundation/backend-model-ownership` | Local identity bootstrap needs internal principal/session checks before external identity provider flows exist. | Foundation bootstrap tests and architecture conformance. | Replace with IdP-backed identity/session commands and scoped read policies. |
| `lib/office_graph/integrations.ex` | `OfficeGraph.Integrations` | `accepted_duplicate/2`, `ash_create/2`, `fetch_source/1`, `get_or_create_source/1`, `verify_duplicate_content/3` | Manual intake idempotency, source lookup, raw archive verification, and normalized event writes | `stabilize-architecture-foundation/backend-model-ownership` | Manual intake owns replay and duplicate checks inside the Integrations boundary before public integration APIs are modeled. | Integration concurrency and walking skeleton tests. | Replace with Ash-managed intake workflow and scoped integration read contracts. |
| `lib/office_graph/operations.ex` | `OfficeGraph.Operations` | `create_operation/5`, `existing_operation/3` | Operation correlation creation and idempotency replay lookup | `stabilize-architecture-foundation/backend-model-ownership` | Operation correlation is infrastructure created for already-authorized commands and must compare idempotency facts before product writes proceed. | Concurrency and operation replay tests. | Introduce policy-aware operation command or explicit internal operation resource action. |
| `lib/office_graph/projections.ex` | `OfficeGraph.Projections` | `fetch_scoped/3`, `graph_relationships_for_links/1`, `read_audit_records/1`, `read_evidence_candidates/2`, `read_graph_items/2`, `read_intake_event/2`, `read_intake_events/1`, `read_proposed_changes/2`, `read_revision_records/1`, `read_verification_checks/2` | Read-only projection assembly | `stabilize-architecture-foundation/ui-projection-contracts` | Projection reads manually assemble policy-filtered mixed view models while resource projection contracts are still being stabilized. | Operator workflow projection and API parity tests. | Replace with policy-aware projection contracts and generated reads where safe. |
| `lib/office_graph/proposed_changes.ex` | `OfficeGraph.ProposedChanges` | `ash_update_internal!/3`, `lock_normalized_event!/3`, `read_existing_for_normalized_event/2`, `validate_accepted_normalized_event/2` | Proposal intake, application, locking, and validation internals | `stabilize-architecture-foundation/backend-model-ownership` | Legacy proposed graph changes remain temporary compatibility data and are coordinated inside the ProposedChanges boundary. | ProposedChanges tests, walking skeleton API tests, and architecture conformance. | Retire with generic proposed graph changes or replace with typed ChangeProposal domain commands. |
| `lib/office_graph/proposed_changes/proposed_graph_change.ex` | `OfficeGraph.ProposedChanges` | `fetch_operation/1`, `fetch_reference/2`, `validate_current_status/2`, `validate_unique_event_change_type/3` | Resource validation reads and internal status validation | `stabilize-architecture-foundation/backend-model-ownership` | Private Ash changes validate proposal references and current status without exposing those reads publicly. | ProposedChanges resource and API tests. | Replace with typed proposal validation actions or delete with legacy proposed graph changes. |
| `lib/office_graph/revisions.ex` | `OfficeGraph.Revisions` | `record!/5`, `count_for_operation/1` | Revision trace write and operation-scoped count | `stabilize-architecture-foundation/backend-model-ownership` | Revisions are internal trace records created after public commands have authorized the product operation. | Architecture conformance plus workflow tests that assert revision traces. | Replace with owner-scoped revision actions or policy-aware revision write commands. |
| `lib/office_graph/runs.ex` | `OfficeGraph.Runs` | `ash_create!/2`, `existing_observation/2`, `existing_observation_for_operation/2`, `existing_run_result/4`, `failed_observations_for_run?/1`, `fetch_scoped/3`, `lock_operation!/1`, `lock_required_checks_for_run!/1`, `lock_run!/1`, `lock_scoped_run!/2`, `mark_required_check_satisfied_in_locked_run!/2`, `packet_has_required_check?/1`, `packet_has_source_reference?/1`, `packet_required_checks/1`, `packet_source_graph_item?/2`, `read_evidence_items/1`, `read_observations/1`, `read_run_required_checks/1`, `read_verification_results/1`, `required_check_graph_item?/2`, `required_checks_for_run/1`, `run_requires_check?/2`, `same_observation_replay?/3`, `set_run_verification_failed/1`, `set_run_verified/1`, `update_run_after_observation!/2`, `update_run_failed!/1` | Run creation, observation replay, required-check recomputation, lock reads, and summary reads | `stabilize-architecture-foundation/backend-model-ownership` | Runs owns internal lifecycle and replay checks after caller authorization and must preserve atomicity while run commands are split. | Work packet run verification, concurrency, and operator workflow tests. | Express run start, observation recording, required-check satisfaction, and recomputation as policy-aware domain commands. |
| `lib/office_graph/runs/changes/validate_observation_run_references.ex` | `OfficeGraph.Runs` | `fetch_scoped_run/3`, `fetch_verification_check/1`, `packet_source_graph_item?/2`, `required_check_graph_item?/2`, `run_requires_check?/2` | Private change validation reads | `stabilize-architecture-foundation/backend-model-ownership` | Observation create validation checks run, packet, source, and verification references inside private resource changes. | Runs resource validation tests and packet-run tests. | Replace with Ash relationships or domain validation contracts. |
| `lib/office_graph/runs/changes/validate_run_required_check_contract.ex` | `OfficeGraph.Runs` | `fetch_scoped_run/3`, `validate_packet_required_check/4` | Private required-check contract validation reads | `stabilize-architecture-foundation/backend-model-ownership` | Required-check creation validates scoped run and packet-version contract inside private resource changes. | Runs resource validation and packet-run tests. | Replace with Ash relationships or domain validation contracts. |
| `lib/office_graph/tenancy.ex` | `OfficeGraph.Tenancy` | `get_or_create!/3`, `insert_then_fetch!/3` | Local tenant bootstrap reads | `stabilize-architecture-foundation/backend-model-ownership` | Local bootstrap needs tenant lookup and creation before tenant provisioning workflow exists. | Foundation bootstrap tests and architecture conformance. | Replace with tenant provisioning or onboarding domain commands. |
| `lib/office_graph/verification.ex` | `OfficeGraph.Verification` | `accept_locked_candidate!/4`, `ash_create!/2`, `existing_acceptance_for_operation/2`, `existing_candidate_for_operation/2`, `fetch_scoped/3`, `lock_candidate!/1`, `lock_operation!/1`, `lock_scoped!/3`, `read_verification_result_for_evidence/1` | Evidence candidate creation, acceptance, replay, locking, and verification result reads | `stabilize-architecture-foundation/backend-model-ownership` | Verification owns evidence acceptance and recomputation internals while these workflows remain transaction-heavy. | Verification, packet-run, and concurrency tests. | Express evidence suggestion, acceptance, and recomputation as policy-aware Verification commands. |
| `lib/office_graph/work_graph.ex` | `OfficeGraph.WorkGraph` | `ash_create_internal/2`, `ash_get/2`, `ash_get_for_update/2`, `ash_update_internal/2`, `lock_review_findings_for_task!/1`, `lock_verification_checks_for_findings!/1` | Graph resource creation, internal lifecycle updates, and parent-child lock reads | `stabilize-architecture-foundation/backend-model-ownership` | WorkGraph owns graph truth writes and lock ordering inside explicit graph transactions. | WorkGraph persistence, authorization, and architecture tests. | Express graph creation and lifecycle transitions as fully Ash-managed WorkGraph commands. |
| `lib/office_graph/work_graph/changes/validate_relationship_endpoints.ex` | `OfficeGraph.WorkGraph` | `fetch_graph_items/1` | Private graph relationship endpoint validation reads | `stabilize-architecture-foundation/backend-model-ownership` | Relationship create validation checks graph item endpoints inside a private resource change. | WorkGraph validation and persistence tests. | Replace with Ash relationships or domain validation contracts. |
| `lib/office_graph/work_graph/changes/validate_same_scope_references.ex` | `OfficeGraph.WorkGraph` | `fetch_reference/2` | Private same-scope reference validation reads | `stabilize-architecture-foundation/backend-model-ownership` | WorkGraph create actions validate referenced records without exposing broad cross-domain reads. | Architecture same-scope validation tests. | Replace with modeled Ash relationships and domain read contracts. |
| `lib/office_graph/work_graph/review_finding.ex` | `OfficeGraph.WorkGraph` | `validate_open_task/2` | Private parent-state validation read | `stabilize-architecture-foundation/backend-model-ownership` | Review finding creation checks parent task state inside the WorkGraph boundary. | WorkGraph persistence tests. | Replace with Ash relationship and invariant change owned by WorkGraph. |
| `lib/office_graph/work_graph/verification_check.ex` | `OfficeGraph.WorkGraph` | `validate_open_review_finding/2` | Private parent-state validation read | `stabilize-architecture-foundation/backend-model-ownership` | Verification check creation checks parent review finding state inside the WorkGraph boundary. | WorkGraph persistence tests. | Replace with Ash relationship and invariant change owned by WorkGraph. |
| `lib/office_graph/work_graph/verification_result.ex` | `OfficeGraph.WorkGraph` | `fetch_evidence_item/1` | Private evidence result validation read | `stabilize-architecture-foundation/backend-model-ownership` | Verification result creation validates accepted evidence references inside WorkGraph resource changes. | Verification and WorkGraph tests. | Move result recording ownership to Verification command and retire WorkGraph-owned result validation. |
| `lib/office_graph/work_packets.ex` | `OfficeGraph.WorkPackets` | `ash_create!/2`, `ash_update!/1`, `existing_packet_result/2`, `lock_operation!/1`, `read_current_version/1`, `read_required_checks/1`, `read_source_references/1` | Packet creation, idempotency replay, operation locking, current version, and readiness reads | `stabilize-architecture-foundation/backend-model-ownership` | WorkPackets owns packet/version/source/check writes and replay while packet creation is still transaction-based. | Work packet verification and concurrency tests. | Express packet creation and readiness as policy-aware WorkPackets commands. |
